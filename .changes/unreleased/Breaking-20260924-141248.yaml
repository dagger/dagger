kind: Breaking
body: |
  `dagger up` is now `dagger start`, with the same flags and arguments.

  `dagger up` no longer runs services. It fails and prints the equivalent
  `dagger start` command.
time: 2026-09-24T14:12:48Z
custom:
  Author: eunomie
  PR: "14328"
