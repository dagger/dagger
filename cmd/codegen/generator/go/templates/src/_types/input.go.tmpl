{{ .Description | Comment }}
type {{ .Name | FormatName }} struct {
{{- range $field := .InputFields }}
{{ $field.Description | Comment }}
{{ $field.Name | FormatName }} {{ $field | FormatInputType }} `json:"{{ $field.Name }}{{if or $field.DefaultValue (and $field.IsOptional $field.Directives.ExpectedType)}},omitempty{{end}}"`
{{- with .Directives.SourceMap -}} // {{ .Module }} ({{ .Filelink | ModuleRelPath }}) {{- end }}
{{ end }}
}
