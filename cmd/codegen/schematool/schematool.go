// Package schematool defines the language-agnostic data shapes the
// Go SDK's AST-based source analyzer (astscan) emits to describe a
// module's declared types.
//
// The full schematool library (schema merging, inspection helpers,
// CLI subcommands) is not part of this minimal landing — only the
// data shapes consumed and produced by astscan live here.
package schematool
