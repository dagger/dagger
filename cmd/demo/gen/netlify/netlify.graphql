scalar FS

type Deploy {
  url: String!
  deployUrl: String!
  logsUrl: String
}

type Netlify {
  deploy(contents: FS!, siteName: String, token: String): Deploy!
}

type Query {
  netlify: Netlify!
}
