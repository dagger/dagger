{{ define "objects" }}
	{{- template "header" -}}
	{{- range . }}
		{{- if HasPrefix .Name "__" }}
			{{- /* we ignore types prefixed by __ */ -}}
		{{- else }}
{{ "" }}		{{- template "type" . }}
{{ "" }}		{{- template "object" . -}}
		{{- end }}
	{{- end }}
{{- end }}
