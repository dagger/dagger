{{ define "field" }}
	{{- if Solve . }}async {{end}}
	{{- .Name -}}
	(
		{{- with .Args -}}
			{{- template "input_args" . -}}
		{{- end -}}
	)
	{{- if Solve . -}}
		{{ "" }} : Promise<Record<string, string>>{{ "" }}
	{{- else -}}
		{{- with .TypeRef }}
			{{- if .Name }}
				{{- template "return" . -}}
			{{ end -}}
		{{- end -}}
	{{- end -}}
{{ end }}
