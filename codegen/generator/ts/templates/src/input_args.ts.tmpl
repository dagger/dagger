{{ define "input_args" -}}
	{{- "" }}args{{ "" -}}
	{{- if IsSingleOptional .Args }}
		{{- "?" -}}
	{{ end }}
	{{- "" }}: {{ .ParentObject.Name | PascalCase }}{{ .Name | PascalCase }}Args{{ "" -}}
{{- end }}
