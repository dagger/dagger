// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// A unique identifier for an object.
type DefaultsID = ID // defaults (../../../../../../../../:0:0)

type Defaults struct { // defaults (../../../../../../../../:0:0)
	query *querybuilder.Selection

	capitalize *string
	greeting   *string
	id         *DefaultsID
	message    *string
}

func (r *Defaults) WithGraphQLQuery(q *querybuilder.Selection) *Defaults {
	return &Defaults{
		query: q,
	}
}

// Capitalize a string
func (r *Defaults) Capitalize(ctx context.Context, s string) (string, error) {
	if r.capitalize != nil {
		return *r.capitalize, nil
	}
	q := r.query.Select("capitalize")
	q = q.Arg("s", s)

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *Defaults) Dir() *Directory {
	q := r.query.Select("dir")

	return &Directory{
		query: q,
	}
}

func (r *Defaults) Docs() *Directory {
	q := r.query.Select("docs")

	return &Directory{
		query: q,
	}
}

func (r *Defaults) File() *File {
	q := r.query.Select("file")

	return &File{
		query: q,
	}
}

func (r *Defaults) Greeting(ctx context.Context) (string, error) {
	if r.greeting != nil {
		return *r.greeting, nil
	}
	q := r.query.Select("greeting")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this Defaults.
func (r *Defaults) ID(ctx context.Context) (DefaultsID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response DefaultsID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Defaults) XXX_GraphQLType() string {
	return "Defaults"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Defaults) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Defaults) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Defaults) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Defaults) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Defaults{query: selectNode(dag.query, id, "Defaults")}
	return nil
}

func (r *Defaults) List(ctx context.Context) ([]string, error) {
	q := r.query.Select("list")

	var response []string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *Defaults) ListSecret(ctx context.Context) ([]Secret, error) {
	q := r.query.Select("listSecret")

	q = q.Select("id")

	type listSecret struct {
		Id SecretID
	}

	convert := func(fields []listSecret) []Secret {
		out := []Secret{}

		for i := range fields {
			val := Secret{id: &fields[i].Id}
			val.query = selectNode(q.Root(), fields[i].Id, "Secret")
			out = append(out, val)
		}

		return out
	}
	var response []listSecret

	q = q.Bind(&response)

	err := q.Execute(ctx)
	if err != nil {
		return nil, err
	}

	return convert(response), nil
}

func (r *Defaults) ListSecrets(ctx context.Context) ([]string, error) {
	q := r.query.Select("listSecrets")

	var response []string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *Defaults) ListString(ctx context.Context) ([]string, error) {
	q := r.query.Select("listString")

	var response []string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// List the contents of a directory
func (r *Defaults) Ls(ctx context.Context, dir *Directory) ([]string, error) {
	assertNotNil("dir", dir)
	q := r.query.Select("ls")
	q = q.Arg("dir", dir)

	var response []string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// List the contents of text files in a directory (with an ignore applied)
func (r *Defaults) LsText(ctx context.Context, dir *Directory) ([]string, error) {
	assertNotNil("dir", dir)
	q := r.query.Select("lsText")
	q = q.Arg("dir", dir)

	var response []string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// DefaultsMessageOpts contains options for Defaults.Message
type DefaultsMessageOpts struct {

	// Default: "world"
	Name string
}

func (r *Defaults) Message(ctx context.Context, opts ...DefaultsMessageOpts) (string, error) {
	if r.message != nil {
		return *r.message, nil
	}
	q := r.query.Select("message")
	for i := len(opts) - 1; i >= 0; i-- {
		// `name` optional argument
		if !querybuilder.IsZeroValue(opts[i].Name) {
			q = q.Arg("name", opts[i].Name)
		}
	}

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *Defaults) Password() *Secret {
	q := r.query.Select("password")

	return &Secret{
		query: q,
	}
}

// DefaultsOpts contains options for Query.Defaults
type DefaultsOpts struct {

	// Default: "hello"
	Greeting string

	Dir *Directory

	Password *Secret

	File *File

	Docs *Directory

	List []string

	Secrets []*Secret
}

func (r *Query) Defaults(opts ...DefaultsOpts) *Defaults { // defaults (../../../../../../../../:0:0)
	q := r.query.Select("defaults")
	for i := len(opts) - 1; i >= 0; i-- {
		// `greeting` optional argument
		if !querybuilder.IsZeroValue(opts[i].Greeting) {
			q = q.Arg("greeting", opts[i].Greeting)
		}
		// `dir` optional argument
		if !querybuilder.IsZeroValue(opts[i].Dir) {
			q = q.Arg("dir", opts[i].Dir)
		}
		// `password` optional argument
		if !querybuilder.IsZeroValue(opts[i].Password) {
			q = q.Arg("password", opts[i].Password)
		}
		// `file` optional argument
		if !querybuilder.IsZeroValue(opts[i].File) {
			q = q.Arg("file", opts[i].File)
		}
		// `docs` optional argument
		if !querybuilder.IsZeroValue(opts[i].Docs) {
			q = q.Arg("docs", opts[i].Docs)
		}
		// `list` optional argument
		if !querybuilder.IsZeroValue(opts[i].List) {
			q = q.Arg("list", opts[i].List)
		}
		// `secrets` optional argument
		if !querybuilder.IsZeroValue(opts[i].Secrets) {
			q = q.Arg("secrets", opts[i].Secrets)
		}
	}

	return &Defaults{
		query: q,
	}
}
