// This class has been generated by dagger-java-sdk. DO NOT EDIT.
package io.dagger.client;

import io.dagger.client.exception.DaggerQueryException;
import jakarta.json.bind.annotation.JsonbTypeDeserializer;
import jakarta.json.bind.annotation.JsonbTypeSerializer;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.Boolean;
import java.lang.InterruptedException;
import java.lang.Override;
import java.lang.String;
import java.lang.reflect.Type;
import java.util.List;
import java.util.concurrent.ExecutionException;

/**
 * A standardized address to load containers, directories, secrets, and other object types. Address format depends on the type, and is validated at type selection.
 */
@JsonbTypeSerializer(IDAbleSerializer.class)
@JsonbTypeDeserializer(Address.Deserializer.class)
public class Address implements Node, IDAble<ID> {
    private QueryBuilder queryBuilder;

    private ID id;

    private String value;

    /**
     * Empty constructor for JSON-B deserialization
     */
    protected Address() {
    }

    Address(QueryBuilder queryBuilder) {
        this.queryBuilder = queryBuilder;
    }

    /**
     * <p>Load a container from the address.</p>
     */
    public Container container() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("container");
        return new Container(nextQueryBuilder);
    }

    /**
     * <p>Load a directory from the address.</p>
     * @param optArgs directory optional arguments
     */
    public Directory directory(DirectoryArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("directory", fieldArgs);
        return new Directory(nextQueryBuilder);
    }

    /**
     * <p>Load a directory from the address.</p>
     */
    public Directory directory() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("directory", fieldArgs);
        return new Directory(nextQueryBuilder);
    }

    /**
     * <p>Load a file from the address.</p>
     * @param optArgs file optional arguments
     */
    public File file(FileArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("file", fieldArgs);
        return new File(nextQueryBuilder);
    }

    /**
     * <p>Load a file from the address.</p>
     */
    public File file() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("file", fieldArgs);
        return new File(nextQueryBuilder);
    }

    /**
     * <p>Load a git ref (branch, tag or commit) from the address.</p>
     */
    public GitRef gitRef() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("gitRef");
        return new GitRef(nextQueryBuilder);
    }

    /**
     * <p>Load a git repository from the address.</p>
     */
    public GitRepository gitRepository() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("gitRepository");
        return new GitRepository(nextQueryBuilder);
    }

    /**
     * <p>A unique identifier for this Address.</p>
     */
    public ID id() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.id != null) {
            return id;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("id");
        return nextQueryBuilder.executeQuery(ID.class);
    }

    /**
     * <p>Load a secret from the address.</p>
     */
    public Secret secret() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("secret");
        return new Secret(nextQueryBuilder);
    }

    /**
     * <p>Load a service from the address.</p>
     */
    public Service service() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("service");
        return new Service(nextQueryBuilder);
    }

    /**
     * <p>Load a local socket from the address.</p>
     */
    public Socket socket() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("socket");
        return new Socket(nextQueryBuilder);
    }

    /**
     * <p>The address value</p>
     */
    public String value() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.value != null) {
            return value;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("value");
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    public static class Deserializer implements JsonbDeserializer<Address> {
        @Override
        public Address deserialize(JsonParser parser, DeserializationContext ctx, Type type) {
            String id = ctx.deserialize(String.class, parser);
            Address o = new Address(Dagger.dag().nodeQueryBuilder("Address", new ID(id)));
            return o;
        }
    }

    /**
     * Optional arguments for {@link Address#directory}
     *
     */
    public static class DirectoryArguments {
        private List<String> exclude;

        private List<String> include;

        private Boolean gitignore;

        private Boolean noCache;

        /**
         *
         */
        public DirectoryArguments withExclude(List<String> exclude) {
            this.exclude = exclude;
            return this;
        }

        /**
         *
         */
        public DirectoryArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        /**
         *
         */
        public DirectoryArguments withGitignore(Boolean gitignore) {
            this.gitignore = gitignore;
            return this;
        }

        /**
         *
         */
        public DirectoryArguments withNoCache(Boolean noCache) {
            this.noCache = noCache;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (exclude != null) {
                builder.add("exclude", this.exclude);
            }

            if (include != null) {
                builder.add("include", this.include);
            }

            if (gitignore != null) {
                builder.add("gitignore", this.gitignore);
            }

            if (noCache != null) {
                builder.add("noCache", this.noCache);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Address#file}
     *
     */
    public static class FileArguments {
        private List<String> exclude;

        private List<String> include;

        private Boolean gitignore;

        private Boolean noCache;

        /**
         *
         */
        public FileArguments withExclude(List<String> exclude) {
            this.exclude = exclude;
            return this;
        }

        /**
         *
         */
        public FileArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        /**
         *
         */
        public FileArguments withGitignore(Boolean gitignore) {
            this.gitignore = gitignore;
            return this;
        }

        /**
         *
         */
        public FileArguments withNoCache(Boolean noCache) {
            this.noCache = noCache;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (exclude != null) {
                builder.add("exclude", this.exclude);
            }

            if (include != null) {
                builder.add("include", this.include);
            }

            if (gitignore != null) {
                builder.add("gitignore", this.gitignore);
            }

            if (noCache != null) {
                builder.add("noCache", this.noCache);
            }

                    return builder.build();
        }
    }
}
