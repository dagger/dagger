// This class has been generated by dagger-java-sdk. DO NOT EDIT.
package io.dagger.client;

import io.dagger.client.exception.DaggerQueryException;
import jakarta.json.bind.annotation.JsonbTypeDeserializer;
import jakarta.json.bind.annotation.JsonbTypeSerializer;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.InterruptedException;
import java.lang.Override;
import java.lang.String;
import java.lang.reflect.Type;
import java.util.concurrent.ExecutionException;

/**
 * A cache storage for the Dagger engine
 */
@JsonbTypeSerializer(IDAbleSerializer.class)
@JsonbTypeDeserializer(EngineCache.Deserializer.class)
public class EngineCache implements Node, IDAble<ID> {
    private QueryBuilder queryBuilder;

    private ID id;

    private Integer maxUsedSpace;

    private Integer minFreeSpace;

    private Void prune;

    private Integer reservedSpace;

    private Integer targetSpace;

    /**
     * Empty constructor for JSON-B deserialization
     */
    protected EngineCache() {
    }

    EngineCache(QueryBuilder queryBuilder) {
        this.queryBuilder = queryBuilder;
    }

    /**
     * <p>The current set of entries in the cache</p>
     * @param optArgs entrySet optional arguments
     */
    public EngineCacheEntrySet entrySet(EntrySetArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("entrySet", fieldArgs);
        return new EngineCacheEntrySet(nextQueryBuilder);
    }

    /**
     * <p>The current set of entries in the cache</p>
     */
    public EngineCacheEntrySet entrySet() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("entrySet", fieldArgs);
        return new EngineCacheEntrySet(nextQueryBuilder);
    }

    /**
     * <p>A unique identifier for this EngineCache.</p>
     */
    public ID id() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.id != null) {
            return id;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("id");
        return nextQueryBuilder.executeQuery(ID.class);
    }

    /**
     * <p>The maximum bytes to keep in the cache without pruning.</p>
     */
    public Integer maxUsedSpace() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.maxUsedSpace != null) {
            return maxUsedSpace;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("maxUsedSpace");
        return nextQueryBuilder.executeQuery(java.lang.Integer.class);
    }

    /**
     * <p>The target amount of free disk space the garbage collector will attempt to leave.</p>
     */
    public Integer minFreeSpace() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.minFreeSpace != null) {
            return minFreeSpace;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("minFreeSpace");
        return nextQueryBuilder.executeQuery(java.lang.Integer.class);
    }

    /**
     * <p>Prune the cache of releaseable entries</p>
     * @param optArgs prune optional arguments
     */
    public Void prune(PruneArguments optArgs) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.prune != null) {
            return prune;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("prune", fieldArgs);
        return nextQueryBuilder.executeQuery(Void.class);
    }

    /**
     * <p>Prune the cache of releaseable entries</p>
     */
    public Void prune() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.prune != null) {
            return prune;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("prune", fieldArgs);
        return nextQueryBuilder.executeQuery(Void.class);
    }

    /**
     * <p>The minimum amount of disk space this policy is guaranteed to retain.</p>
     */
    public Integer reservedSpace() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.reservedSpace != null) {
            return reservedSpace;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("reservedSpace");
        return nextQueryBuilder.executeQuery(java.lang.Integer.class);
    }

    /**
     * <p>The target number of bytes to keep when pruning.</p>
     */
    public Integer targetSpace() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.targetSpace != null) {
            return targetSpace;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("targetSpace");
        return nextQueryBuilder.executeQuery(java.lang.Integer.class);
    }

    public static class Deserializer implements JsonbDeserializer<EngineCache> {
        @Override
        public EngineCache deserialize(JsonParser parser, DeserializationContext ctx, Type type) {
            String id = ctx.deserialize(String.class, parser);
            EngineCache o = new EngineCache(Dagger.dag().nodeQueryBuilder("EngineCache", new ID(id)));
            return o;
        }
    }

    /**
     * Optional arguments for {@link EngineCache#entrySet}
     *
     */
    public static class EntrySetArguments {
        private String key;

        /**
         *
         */
        public EntrySetArguments withKey(String key) {
            this.key = key;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (key != null) {
                builder.add("key", this.key);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link EngineCache#prune}
     *
     */
    public static class PruneArguments {
        private Boolean useDefaultPolicy;

        private String maxUsedSpace;

        private String reservedSpace;

        private String minFreeSpace;

        private String targetSpace;

        /**
         * Use the engine-wide default pruning policy if true, otherwise prune the whole cache of any releasable entries.
         */
        public PruneArguments withUseDefaultPolicy(Boolean useDefaultPolicy) {
            this.useDefaultPolicy = useDefaultPolicy;
            return this;
        }

        /**
         * Override the maximum disk space to keep before pruning (e.g. "200GB" or "80%").
         */
        public PruneArguments withMaxUsedSpace(String maxUsedSpace) {
            this.maxUsedSpace = maxUsedSpace;
            return this;
        }

        /**
         * Override the minimum disk space to retain during pruning (e.g. "500GB" or "10%").
         */
        public PruneArguments withReservedSpace(String reservedSpace) {
            this.reservedSpace = reservedSpace;
            return this;
        }

        /**
         * Override the minimum free disk space target during pruning (e.g. "20GB" or "20%").
         */
        public PruneArguments withMinFreeSpace(String minFreeSpace) {
            this.minFreeSpace = minFreeSpace;
            return this;
        }

        /**
         * Override the target disk space to keep after pruning (e.g. "200GB" or "50%").
         */
        public PruneArguments withTargetSpace(String targetSpace) {
            this.targetSpace = targetSpace;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (useDefaultPolicy != null) {
                builder.add("useDefaultPolicy", this.useDefaultPolicy);
            }

            if (maxUsedSpace != null) {
                builder.add("maxUsedSpace", this.maxUsedSpace);
            }

            if (reservedSpace != null) {
                builder.add("reservedSpace", this.reservedSpace);
            }

            if (minFreeSpace != null) {
                builder.add("minFreeSpace", this.minFreeSpace);
            }

            if (targetSpace != null) {
                builder.add("targetSpace", this.targetSpace);
            }

                    return builder.build();
        }
    }
}
