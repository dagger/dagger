// This class has been generated by dagger-java-sdk. DO NOT EDIT.
package io.dagger.client;

import io.dagger.client.exception.DaggerQueryException;
import jakarta.json.bind.annotation.JsonbTypeDeserializer;
import jakarta.json.bind.annotation.JsonbTypeSerializer;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.Boolean;
import java.lang.InterruptedException;
import java.lang.Override;
import java.lang.String;
import java.lang.reflect.Type;
import java.util.List;
import java.util.concurrent.ExecutionException;

/**
 * A collection of environment variables.
 */
@JsonbTypeSerializer(IDAbleSerializer.class)
@JsonbTypeDeserializer(EnvFile.Deserializer.class)
public class EnvFile implements Node, IDAble<ID> {
    private QueryBuilder queryBuilder;

    private Boolean exists;

    private String get;

    private ID id;

    /**
     * Empty constructor for JSON-B deserialization
     */
    protected EnvFile() {
    }

    EnvFile(QueryBuilder queryBuilder) {
        this.queryBuilder = queryBuilder;
    }

    /**
     * <p>Return as a file</p>
     */
    public File asFile() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("asFile");
        return new File(nextQueryBuilder);
    }

    /**
     * <p>Check if a variable exists</p>
     * @param name Variable name
     */
    public Boolean exists(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.exists != null) {
            return exists;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("exists", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.Boolean.class);
    }

    /**
     * <p>Lookup a variable (last occurrence wins) and return its value, or an empty string</p>
     * @param name Variable name
     * @param optArgs get optional arguments
     */
    public String get(String name, GetArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.get != null) {
            return get;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("get", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Lookup a variable (last occurrence wins) and return its value, or an empty string</p>
     * @param name Variable name
     */
    public String get(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.get != null) {
            return get;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("get", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>A unique identifier for this EnvFile.</p>
     */
    public ID id() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.id != null) {
            return id;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("id");
        return nextQueryBuilder.executeQuery(ID.class);
    }

    /**
     * <p>Filters variables by prefix and removes the pref from keys. Variables without the prefix are excluded. For example, with the prefix "MY_APP_" and variables: MY_APP_TOKEN=topsecret MY_APP_NAME=hello FOO=bar the resulting environment will contain: TOKEN=topsecret NAME=hello</p>
     * @param prefix The prefix to filter by
     */
    public EnvFile namespace(String prefix) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("prefix", prefix);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("namespace", fieldArgs);
        return new EnvFile(nextQueryBuilder);
    }

    /**
     * <p>Return all variables</p>
     * @param optArgs variables optional arguments
     */
    public List<EnvVariable> variables(VariablesArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("variables", fieldArgs);
        nextQueryBuilder = nextQueryBuilder.chain(List.of("id"));
        List<QueryBuilder> builders = nextQueryBuilder.executeObjectListQuery("EnvVariable");
        return builders.stream().map(qb -> new EnvVariable(qb)).toList();
    }

    /**
     * <p>Return all variables</p>
     */
    public List<EnvVariable> variables() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("variables", fieldArgs);
        nextQueryBuilder = nextQueryBuilder.chain(List.of("id"));
        List<QueryBuilder> builders = nextQueryBuilder.executeObjectListQuery("EnvVariable");
        return builders.stream().map(qb -> new EnvVariable(qb)).toList();
    }

    /**
     * <p>Add a variable</p>
     * @param name Variable name
     * @param value Variable value
     */
    public EnvFile withVariable(String name, String value) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        builder.add("value", value);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("withVariable", fieldArgs);
        return new EnvFile(nextQueryBuilder);
    }

    /**
     * <p>Remove all occurrences of the named variable</p>
     * @param name Variable name
     */
    public EnvFile withoutVariable(String name) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("withoutVariable", fieldArgs);
        return new EnvFile(nextQueryBuilder);
    }

    public static class Deserializer implements JsonbDeserializer<EnvFile> {
        @Override
        public EnvFile deserialize(JsonParser parser, DeserializationContext ctx, Type type) {
            String id = ctx.deserialize(String.class, parser);
            EnvFile o = new EnvFile(Dagger.dag().nodeQueryBuilder("EnvFile", new ID(id)));
            return o;
        }
    }

    /**
     * Optional arguments for {@link EnvFile#get}
     *
     */
    public static class GetArguments {
        private Boolean raw;

        /**
         * Return the value exactly as written to the file. No quote removal or variable expansion
         */
        public GetArguments withRaw(Boolean raw) {
            this.raw = raw;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (raw != null) {
                builder.add("raw", this.raw);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link EnvFile#variables}
     *
     */
    public static class VariablesArguments {
        private Boolean raw;

        /**
         * Return values exactly as written to the file. No quote removal or variable expansion
         */
        public VariablesArguments withRaw(Boolean raw) {
            this.raw = raw;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (raw != null) {
                builder.add("raw", this.raw);
            }

                    return builder.build();
        }
    }
}
