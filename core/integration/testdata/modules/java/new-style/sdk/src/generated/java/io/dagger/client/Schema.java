// This class has been generated by dagger-java-sdk. DO NOT EDIT.
package io.dagger.client;

import io.dagger.client.exception.DaggerQueryException;
import jakarta.json.bind.annotation.JsonbTypeDeserializer;
import jakarta.json.bind.annotation.JsonbTypeSerializer;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.InterruptedException;
import java.lang.Override;
import java.lang.String;
import java.lang.reflect.Type;
import java.util.concurrent.ExecutionException;

/**
 * A GraphQL introspection schema that can be inspected and merged.
 */
@JsonbTypeSerializer(IDAbleSerializer.class)
@JsonbTypeDeserializer(Schema.Deserializer.class)
public class Schema implements Node, IDAble<ID> {
    private QueryBuilder queryBuilder;

    private JSON contents;

    private ID id;

    /**
     * Empty constructor for JSON-B deserialization
     */
    protected Schema() {
    }

    Schema(QueryBuilder queryBuilder) {
        this.queryBuilder = queryBuilder;
    }

    /**
     * <p>Serialize the schema back to introspection JSON.</p>
     */
    public JSON contents() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.contents != null) {
            return contents;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("contents");
        return nextQueryBuilder.executeQuery(JSON.class);
    }

    /**
     * <p>A unique identifier for this Schema.</p>
     */
    public ID id() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.id != null) {
            return id;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("id");
        return nextQueryBuilder.executeQuery(ID.class);
    }

    /**
     * <p>Merge a module's introspection-shaped type definitions into the schema, returning the combined schema.</p>
     * @param moduleTypes Introspection JSON describing the types the module defines. Object, interface and enum types are appended to the schema, and a constructor field for the module is added to the Query type.
     * @param moduleName The name of the module whose types are being merged. Used to stamp the @sourceModuleName directive and to derive the module's constructor field.
     */
    public Schema merge(JSON moduleTypes, String moduleName) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("moduleTypes", moduleTypes);
        builder.add("moduleName", moduleName);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("merge", fieldArgs);
        return new Schema(nextQueryBuilder);
    }

    public static class Deserializer implements JsonbDeserializer<Schema> {
        @Override
        public Schema deserialize(JsonParser parser, DeserializationContext ctx, Type type) {
            String id = ctx.deserialize(String.class, parser);
            Schema o = new Schema(Dagger.dag().nodeQueryBuilder("Schema", new ID(id)));
            return o;
        }
    }
}
