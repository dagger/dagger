// This class has been generated by dagger-java-sdk. DO NOT EDIT.
package io.dagger.client;

import io.dagger.client.exception.DaggerQueryException;
import jakarta.json.bind.annotation.JsonbTypeDeserializer;
import jakarta.json.bind.annotation.JsonbTypeSerializer;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.Boolean;
import java.lang.InterruptedException;
import java.lang.Override;
import java.lang.String;
import java.lang.reflect.Type;
import java.util.List;
import java.util.concurrent.ExecutionException;

/**
 * A Dagger workspace detected from the current working directory.
 */
@JsonbTypeSerializer(IDAbleSerializer.class)
@JsonbTypeDeserializer(Workspace.Deserializer.class)
public class Workspace implements Node, IDAble<ID> {
    private QueryBuilder queryBuilder;

    private String address;

    private String clientId;

    private String configFile;

    private String configRead;

    private String configWrite;

    private String cwd;

    private String envCreate;

    private String envRemove;

    private String findUp;

    private ID id;

    private String init;

    private String install;

    private String moduleInit;

    private String uninstall;

    /**
     * Empty constructor for JSON-B deserialization
     */
    protected Workspace() {
    }

    Workspace(QueryBuilder queryBuilder) {
        this.queryBuilder = queryBuilder;
    }

    /**
     * <p>Canonical Dagger address of the workspace location.</p>
     */
    public String address() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.address != null) {
            return address;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("address");
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Return all checks from modules loaded in the workspace.</p>
     * @param optArgs checks optional arguments
     */
    public CheckGroup checks(ChecksArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("checks", fieldArgs);
        return new CheckGroup(nextQueryBuilder);
    }

    /**
     * <p>Return all checks from modules loaded in the workspace.</p>
     */
    public CheckGroup checks() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("checks", fieldArgs);
        return new CheckGroup(nextQueryBuilder);
    }

    /**
     * <p>The client ID that owns this workspace's host filesystem.</p>
     */
    public String clientId() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.clientId != null) {
            return clientId;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("clientId");
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Selected native workspace config file relative to the workspace root, if any.</p>
     */
    public String configFile() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.configFile != null) {
            return configFile;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("configFile");
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Read a configuration value from config.toml.<br/><br/>If key is empty, returns the full config.<br/><br/>If key points to a scalar, returns the value.<br/><br/>If key points to a table, returns flattened dotted-key output.</p>
     * @param optArgs configRead optional arguments
     */
    public String configRead(ConfigReadArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.configRead != null) {
            return configRead;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("configRead", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Read a configuration value from config.toml.<br/><br/>If key is empty, returns the full config.<br/><br/>If key points to a scalar, returns the value.<br/><br/>If key points to a table, returns flattened dotted-key output.</p>
     */
    public String configRead() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.configRead != null) {
            return configRead;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("configRead", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Write a configuration value to config.toml.</p>
     * @param key Dotted key path (e.g. modules.greeter.source).
     * @param value Value to set. Bools, integers, and comma-separated arrays are auto-detected.
     * @param optArgs configWrite optional arguments
     */
    public String configWrite(String key, String value, ConfigWriteArguments optArgs) throws
            InterruptedException, ExecutionException, DaggerQueryException {
        if (this.configWrite != null) {
            return configWrite;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("key", key);
        builder.add("value", value);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("configWrite", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Write a configuration value to config.toml.</p>
     * @param key Dotted key path (e.g. modules.greeter.source).
     * @param value Value to set. Bools, integers, and comma-separated arrays are auto-detected.
     */
    public String configWrite(String key, String value) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.configWrite != null) {
            return configWrite;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("key", key);
        builder.add("value", value);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("configWrite", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Current location within the workspace root. Relative paths in workspace APIs resolve from here.</p>
     */
    public String cwd() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.cwd != null) {
            return cwd;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("cwd");
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Returns a Directory from the workspace.<br/><br/>Relative paths resolve from the workspace cwd. Absolute paths resolve from the workspace root.</p>
     * @param path Location of the directory to retrieve. Relative paths (e.g., "src") resolve from the workspace cwd; absolute paths (e.g., "/src") resolve from the workspace root.
     * @param optArgs directory optional arguments
     */
    public Directory directory(String path, DirectoryArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("path", path);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("directory", fieldArgs);
        return new Directory(nextQueryBuilder);
    }

    /**
     * <p>Returns a Directory from the workspace.<br/><br/>Relative paths resolve from the workspace cwd. Absolute paths resolve from the workspace root.</p>
     * @param path Location of the directory to retrieve. Relative paths (e.g., "src") resolve from the workspace cwd; absolute paths (e.g., "/src") resolve from the workspace root.
     */
    public Directory directory(String path) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("path", path);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("directory", fieldArgs);
        return new Directory(nextQueryBuilder);
    }

    /**
     * <p>Create a named workspace environment if it does not already exist.</p>
     * @param name Environment name.
     * @param optArgs envCreate optional arguments
     */
    public String envCreate(String name, EnvCreateArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.envCreate != null) {
            return envCreate;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("envCreate", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Create a named workspace environment if it does not already exist.</p>
     * @param name Environment name.
     */
    public String envCreate(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.envCreate != null) {
            return envCreate;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("envCreate", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>List named environments defined in the workspace configuration.</p>
     */
    public List<String> envList() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("envList");
        return nextQueryBuilder.executeListQuery(String.class);
    }

    /**
     * <p>Remove a named workspace environment.</p>
     * @param name Environment name.
     * @param optArgs envRemove optional arguments
     */
    public String envRemove(String name, EnvRemoveArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.envRemove != null) {
            return envRemove;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("envRemove", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Remove a named workspace environment.</p>
     * @param name Environment name.
     */
    public String envRemove(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.envRemove != null) {
            return envRemove;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("envRemove", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Returns a File from the workspace.<br/><br/>Relative paths resolve from the workspace cwd. Absolute paths resolve from the workspace root.</p>
     * @param path Location of the file to retrieve. Relative paths (e.g., "go.mod") resolve from the workspace cwd; absolute paths (e.g., "/go.mod") resolve from the workspace root.
     */
    public File file(String path) {
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("path", path);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("file", fieldArgs);
        return new File(nextQueryBuilder);
    }

    /**
     * <p>Search for a file or directory by walking up from the start path within the workspace.<br/><br/>Returns the absolute workspace path if found, or null if not found.<br/><br/>Relative start paths resolve from the workspace cwd.<br/><br/>The search stops at the workspace root and will not traverse above it.</p>
     * @param name The name of the file or directory to search for.
     * @param optArgs findUp optional arguments
     */
    public String findUp(String name, FindUpArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.findUp != null) {
            return findUp;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("findUp", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Search for a file or directory by walking up from the start path within the workspace.<br/><br/>Returns the absolute workspace path if found, or null if not found.<br/><br/>Relative start paths resolve from the workspace cwd.<br/><br/>The search stops at the workspace root and will not traverse above it.</p>
     * @param name The name of the file or directory to search for.
     */
    public String findUp(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.findUp != null) {
            return findUp;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("findUp", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Return all generators from modules loaded in the workspace.</p>
     * @param optArgs generators optional arguments
     */
    public GeneratorGroup generators(GeneratorsArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("generators", fieldArgs);
        return new GeneratorGroup(nextQueryBuilder);
    }

    /**
     * <p>Return all generators from modules loaded in the workspace.</p>
     */
    public GeneratorGroup generators() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("generators", fieldArgs);
        return new GeneratorGroup(nextQueryBuilder);
    }

    /**
     * <p>Git state for this workspace. Errors if the workspace is not in a git repository.</p>
     */
    public WorkspaceGit git() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("git");
        return new WorkspaceGit(nextQueryBuilder);
    }

    /**
     * <p>A unique identifier for this Workspace.</p>
     */
    public ID id() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.id != null) {
            return id;
        }
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("id");
        return nextQueryBuilder.executeQuery(ID.class);
    }

    /**
     * <p>Initialize workspace config, creating .dagger/config.toml.</p>
     * @param optArgs init optional arguments
     */
    public String init(InitArguments optArgs) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.init != null) {
            return init;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("init", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Initialize workspace config, creating .dagger/config.toml.</p>
     */
    public String init() throws InterruptedException, ExecutionException, DaggerQueryException {
        if (this.init != null) {
            return init;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("init", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Install a module into the workspace, writing config.toml to the host.</p>
     * @param ref Module reference to install.
     * @param optArgs install optional arguments
     */
    public String install(String ref, InstallArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.install != null) {
            return install;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("ref", ref);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("install", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Install a module into the workspace, writing config.toml to the host.</p>
     * @param ref Module reference to install.
     */
    public String install(String ref) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.install != null) {
            return install;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("ref", ref);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("install", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Plan the explicit migration needed for the current workspace.<br/><br/>The returned plan has an empty changeset and no steps when no migration is needed.</p>
     * @param optArgs migrate optional arguments
     */
    public WorkspaceMigration migrate(MigrateArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("migrate", fieldArgs);
        return new WorkspaceMigration(nextQueryBuilder);
    }

    /**
     * <p>Plan the explicit migration needed for the current workspace.<br/><br/>The returned plan has an empty changeset and no steps when no migration is needed.</p>
     */
    public WorkspaceMigration migrate() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("migrate", fieldArgs);
        return new WorkspaceMigration(nextQueryBuilder);
    }

    /**
     * <p>Create a new module owned by the workspace and auto-install it in config.toml.</p>
     * @param name Name of the new module.
     * @param optArgs moduleInit optional arguments
     */
    public String moduleInit(String name, ModuleInitArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.moduleInit != null) {
            return moduleInit;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("moduleInit", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Create a new module owned by the workspace and auto-install it in config.toml.</p>
     * @param name Name of the new module.
     */
    public String moduleInit(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.moduleInit != null) {
            return moduleInit;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("moduleInit", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>List modules defined in the workspace configuration.</p>
     * @param optArgs moduleList optional arguments
     */
    public List<WorkspaceModule> moduleList(ModuleListArguments optArgs) throws
            InterruptedException, ExecutionException, DaggerQueryException {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("moduleList", fieldArgs);
        nextQueryBuilder = nextQueryBuilder.chain(List.of("id"));
        List<QueryBuilder> builders = nextQueryBuilder.executeObjectListQuery("WorkspaceModule");
        return builders.stream().map(qb -> new WorkspaceModule(qb)).toList();
    }

    /**
     * <p>List modules defined in the workspace configuration.</p>
     */
    public List<WorkspaceModule> moduleList() throws InterruptedException, ExecutionException,
            DaggerQueryException {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("moduleList", fieldArgs);
        nextQueryBuilder = nextQueryBuilder.chain(List.of("id"));
        List<QueryBuilder> builders = nextQueryBuilder.executeObjectListQuery("WorkspaceModule");
        return builders.stream().map(qb -> new WorkspaceModule(qb)).toList();
    }

    /**
     * <p>Refresh lock entries for selected workspace-config modules.<br/><br/>This layers selective workspace refresh on top of the lockfile base.</p>
     * @param optArgs refreshModules optional arguments
     */
    public Changeset refreshModules(RefreshModulesArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("refreshModules", fieldArgs);
        return new Changeset(nextQueryBuilder);
    }

    /**
     * <p>Refresh lock entries for selected workspace-config modules.<br/><br/>This layers selective workspace refresh on top of the lockfile base.</p>
     */
    public Changeset refreshModules() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("refreshModules", fieldArgs);
        return new Changeset(nextQueryBuilder);
    }

    /**
     * <p>Return all services from modules loaded in the workspace.</p>
     * @param optArgs services optional arguments
     */
    public UpGroup services(ServicesArguments optArgs) {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("services", fieldArgs);
        return new UpGroup(nextQueryBuilder);
    }

    /**
     * <p>Return all services from modules loaded in the workspace.</p>
     */
    public UpGroup services() {
        Arguments.Builder builder = Arguments.newBuilder();
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("services", fieldArgs);
        return new UpGroup(nextQueryBuilder);
    }

    /**
     * <p>Uninstall a module from the workspace, writing config.toml to the host.</p>
     * @param name Name of the installed module entry to remove.
     * @param optArgs uninstall optional arguments
     */
    public String uninstall(String name, UninstallArguments optArgs) throws InterruptedException,
            ExecutionException, DaggerQueryException {
        if (this.uninstall != null) {
            return uninstall;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        fieldArgs = fieldArgs.merge(optArgs.toArguments());
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("uninstall", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Uninstall a module from the workspace, writing config.toml to the host.</p>
     * @param name Name of the installed module entry to remove.
     */
    public String uninstall(String name) throws InterruptedException, ExecutionException,
            DaggerQueryException {
        if (this.uninstall != null) {
            return uninstall;
        }
        Arguments.Builder builder = Arguments.newBuilder();
        builder.add("name", name);
        Arguments fieldArgs = builder.build();
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("uninstall", fieldArgs);
        return nextQueryBuilder.executeQuery(java.lang.String.class);
    }

    /**
     * <p>Refresh workspace-managed state and return the resulting changeset.<br/><br/>Currently this refreshes existing lockfile entries only.</p>
     */
    public Changeset update() {
        QueryBuilder nextQueryBuilder = this.queryBuilder.chain("update");
        return new Changeset(nextQueryBuilder);
    }

    public static class Deserializer implements JsonbDeserializer<Workspace> {
        @Override
        public Workspace deserialize(JsonParser parser, DeserializationContext ctx, Type type) {
            String id = ctx.deserialize(String.class, parser);
            Workspace o = new Workspace(Dagger.dag().nodeQueryBuilder("Workspace", new ID(id)));
            return o;
        }
    }

    /**
     * Optional arguments for {@link Workspace#checks}
     *
     */
    public static class ChecksArguments {
        private List<String> include;

        private Boolean noGenerate;

        private Boolean onlyGenerate;

        /**
         * Only include checks matching the specified patterns
         */
        public ChecksArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        /**
         * When true, only return annotated check functions; exclude generate-as-checks
         */
        public ChecksArguments withNoGenerate(Boolean noGenerate) {
            this.noGenerate = noGenerate;
            return this;
        }

        /**
         * When true, only return generate-as-checks; exclude annotated check functions
         */
        public ChecksArguments withOnlyGenerate(Boolean onlyGenerate) {
            this.onlyGenerate = onlyGenerate;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (include != null) {
                builder.add("include", this.include);
            }

            if (noGenerate != null) {
                builder.add("noGenerate", this.noGenerate);
            }

            if (onlyGenerate != null) {
                builder.add("onlyGenerate", this.onlyGenerate);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#configRead}
     *
     */
    public static class ConfigReadArguments {
        private String key;

        /**
         * Dotted key path (e.g. modules.greeter.source). Empty for full config.
         */
        public ConfigReadArguments withKey(String key) {
            this.key = key;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (key != null) {
                builder.add("key", this.key);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#configWrite}
     *
     */
    public static class ConfigWriteArguments {
        private Boolean here;

        /**
         * Write to the workspace config directory at the workspace cwd.
         */
        public ConfigWriteArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#directory}
     *
     */
    public static class DirectoryArguments {
        private List<String> exclude;

        private List<String> include;

        private Boolean gitignore;

        /**
         * Exclude artifacts that match the given pattern (e.g., ["node_modules/", ".git*"]).
         */
        public DirectoryArguments withExclude(List<String> exclude) {
            this.exclude = exclude;
            return this;
        }

        /**
         * Include only artifacts that match the given pattern (e.g., ["app/", "package.*"]).
         */
        public DirectoryArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        /**
         * Apply .gitignore filter rules inside the directory.
         */
        public DirectoryArguments withGitignore(Boolean gitignore) {
            this.gitignore = gitignore;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (exclude != null) {
                builder.add("exclude", this.exclude);
            }

            if (include != null) {
                builder.add("include", this.include);
            }

            if (gitignore != null) {
                builder.add("gitignore", this.gitignore);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#envCreate}
     *
     */
    public static class EnvCreateArguments {
        private Boolean here;

        /**
         * Write to the workspace config directory at the workspace cwd.
         */
        public EnvCreateArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#envRemove}
     *
     */
    public static class EnvRemoveArguments {
        private Boolean here;

        /**
         * Write to the workspace config directory at the workspace cwd.
         */
        public EnvRemoveArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#findUp}
     *
     */
    public static class FindUpArguments {
        private String from;

        /**
         * Path to start the search from. Relative paths resolve from the workspace cwd; absolute paths resolve from the workspace root.
         */
        public FindUpArguments withFrom(String from) {
            this.from = from;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (from != null) {
                builder.add("from", this.from);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#generators}
     *
     */
    public static class GeneratorsArguments {
        private List<String> include;

        /**
         * Only include generators matching the specified patterns
         */
        public GeneratorsArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (include != null) {
                builder.add("include", this.include);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#init}
     *
     */
    public static class InitArguments {
        private Boolean here;

        /**
         * Create the workspace config directory at the workspace cwd instead of using the default write target.
         */
        public InitArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#install}
     *
     */
    public static class InstallArguments {
        private String name;

        private Boolean here;

        /**
         * Override name for the installed module entry.
         */
        public InstallArguments withName(String name) {
            this.name = name;
            return this;
        }

        /**
         * Write to the workspace config directory at the workspace cwd.
         */
        public InstallArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (name != null) {
                builder.add("name", this.name);
            }

            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#migrate}
     *
     */
    public static class MigrateArguments {
        private Boolean force;

        /**
         *
         */
        public MigrateArguments withForce(Boolean force) {
            this.force = force;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (force != null) {
                builder.add("force", this.force);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#moduleInit}
     *
     */
    public static class ModuleInitArguments {
        private String sdk;

        private String source;

        private List<String> include;

        private Boolean selfCalls;

        private Boolean here;

        /**
         * SDK to use for the new module.
         */
        public ModuleInitArguments withSdk(String sdk) {
            this.sdk = sdk;
            return this;
        }

        /**
         * Source subpath within the new module.
         */
        public ModuleInitArguments withSource(String source) {
            this.source = source;
            return this;
        }

        /**
         * Additional include patterns for the module.
         */
        public ModuleInitArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        /**
         * Enable the self-calls experimental feature for the new module.
         */
        public ModuleInitArguments withSelfCalls(Boolean selfCalls) {
            this.selfCalls = selfCalls;
            return this;
        }

        /**
         * Write to the workspace config directory at the workspace cwd.
         */
        public ModuleInitArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (sdk != null) {
                builder.add("sdk", this.sdk);
            }

            if (source != null) {
                builder.add("source", this.source);
            }

            if (include != null) {
                builder.add("include", this.include);
            }

            if (selfCalls != null) {
                builder.add("selfCalls", this.selfCalls);
            }

            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#moduleList}
     *
     */
    public static class ModuleListArguments {
        private String module;

        /**
         * Optional module alias to inspect.
         */
        public ModuleListArguments withModule(String module) {
            this.module = module;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (module != null) {
                builder.add("module", this.module);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#refreshModules}
     *
     */
    public static class RefreshModulesArguments {
        private List<String> moduleNames;

        /**
         * Workspace module names to refresh.
         */
        public RefreshModulesArguments withModuleNames(List<String> moduleNames) {
            this.moduleNames = moduleNames;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (moduleNames != null) {
                builder.add("moduleNames", this.moduleNames);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#services}
     *
     */
    public static class ServicesArguments {
        private List<String> include;

        /**
         * Only include services matching the specified patterns
         */
        public ServicesArguments withInclude(List<String> include) {
            this.include = include;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (include != null) {
                builder.add("include", this.include);
            }

                    return builder.build();
        }
    }

    /**
     * Optional arguments for {@link Workspace#uninstall}
     *
     */
    public static class UninstallArguments {
        private Boolean here;

        /**
         * Write to the workspace config directory at the workspace cwd.
         */
        public UninstallArguments withHere(Boolean here) {
            this.here = here;
            return this;
        }

        Arguments toArguments() {
            Arguments.Builder builder = Arguments.newBuilder();
            if (here != null) {
                builder.add("here", this.here);
            }

                    return builder.build();
        }
    }
}
