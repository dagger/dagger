[project]
name = "basic"
version = "0.0.0"
dependencies = [
    "dagger-io @ {root:uri}/../../../../../../sdk/python"
]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
