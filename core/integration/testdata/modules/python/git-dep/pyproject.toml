[project]
name = "main"
version = "0.0.0"
dependencies = [
    "asyncer @ git+https://github.com/tiangolo/asyncer@0.0.5",
]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

