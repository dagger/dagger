"""A deliberately malformed service module (@start with a required arg) for validation tests."""
type BadupArg {
  """@start with a required arg, which the contract forbids."""
  web(image: String!): Service! @start {
    container.from(image).withExposedPort(80).asService
  }
}
