"""A deliberately malformed service module (@up returning a non-Service) for validation tests."""
type BadupReturn {
  """@up that does not return Service!, which the contract forbids."""
  @up
  pub web: Container! {
    container.from("nginx:alpine")
  }
}
