type Searcher {
  pub file_paths: [String!]!

  new(ws: Workspace!, pattern: String!, limit: Int! = 1000, paths: [String!] = []) {
    self.file_paths = ws.search(pattern: pattern, limit: limit, paths: paths).{{filePath, lineNumber}}.map { r => `${r.filePath}:${r.lineNumber}` }
    self
  }
}
