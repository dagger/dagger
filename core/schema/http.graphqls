extend type Query {
    """
    Returns a file containing an http remote url content.
    Example:
    http("https://docs.dagger.io")
    """
    http(
        "Http url to get the content from."
        url: String!
    ): File!
}
