extend type Query {
    """
    Returns a file containing an http remote url content.

    Example:
    http(url: "https://docs.dagger.io")
    """
    http(
        """
        Http url to get the content from (e.g., "https://docs.dagger.io").
        """
        url: String!
    ): File!
}
