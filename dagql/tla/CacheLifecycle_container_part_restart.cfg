\* Imported metadata and one saved snapshot; local opening, retry, release,
\* retained closure, restart, and a second unopened flush. Expected: pass.
\* One retained result, metadata plus fs, two evaluators. One Restart and
\* optional second Flush. 2026-09-05: 13,501,639 distinct states / 146.81s.
\* BFS probes reach stored open, join, retry, release, actual restart open,
\* and typed/envelope second flush. Named breaks and exact counts: README.md.
SPECIFICATION Spec
CONSTANTS
    Sessions = {s1}
    s1 = s1
    Calls = {cA}
    cA = cA
    ClassOf <- OneClass
    MaxInvocations = 1
    MaxResults = 1
    Handles = {}
    AllowRelease = TRUE
    ReleaseSessions <- AllSessions
    PersistableIntent = TRUE
    DrainOnRelease = TRUE
    AllowPruneCut = FALSE
    ModelLazy = TRUE
    MaxEvals = 2
    LazyGroups <- RestoreGroups
    LazyParts <- ContainerPartsTwo
    PartGroupOf <- ContainerPartsTwoGroupOf
    GroupNeeds <- RestoreGroupNeeds
    ModelContainerPartPersistence = TRUE
    ModelPartDelegation = FALSE
    ModelPersistence = TRUE
    ImportInit = TRUE
    ModelNestedCalls = FALSE
    ModelLateDeps = FALSE
    FnCanFail = FALSE
    AttachCanFail = FALSE
    LeaseCanFail = FALSE
    ReaderCanCancel = FALSE
    LazyCanFail = TRUE
    DecodeCanFail = TRUE
INVARIANTS TypeOK OwnershipExact ReturnedLive LazyAttemptDefersCollection EvalDoneComplete LazyCompleteSettled LazyMutualExclusion LazySuccessPermanent NoStaleCancelError PartServedByItsGroup GroupOrderRespected SweepOwnerActive SweepStartsFinal FlushCleanCapture FlushReferentialIntegrity ContainerCaptureExact ContainerSweepCaptureExact ContainerJointCompletion StoredPartsNeverComputed StoredOpenRequiresDemand StoredOpenNotRepeated ContainerPartReturnedReady ContainerDecodePreserves ContainerOriginalGroupsSeeded ContainerClosureComplete
