\* Cross-result delegation and final parent copy sweeps. Each result has
\* metadata plus one snapshot delegation group. Two invocations can publish
\* a child with a dependency on an eager or lazy parent. The evaluator bound
\* allows an external child demand and the sweep's internal parent demand.
\* The copied child part needs no external caller. GroupNeeds keeps ordinary
\* cache attempts metadata-first; direct copies need object consumption.
\*
\* Scope: release and persisted edges are off. The previous single-group
\* release scenario passed at 37,417,395 distinct states / 4m54s (2026-08-31).
\* Its multi-group attempts exceeded 45M and 79M states without finishing.
\* The two-group sweep shape without call symmetry passed once at 45,036,407
\* distinct states / 14m52s (2026-09-04). SymmCalls removes only renamings of
\* the two interchangeable call identities. The routine bound is below.
\* Recorded (2026-09-04): 22,518,204 distinct states / 6m53s. Expected: pass.
\*
\* Reachability probes (2026-09-04), injected with tla-check one:
\*   NoSweep == \A e \in EvalIds : ~evals[e].sweep
\* fails at depth 24. EvalSweepDelegation starts the copy and parent demand.
\*   NoSweptCopy == \A e \in EvalIds : evals[e].sweep =>
\*       ~res[evals[e].fromRes].lazyConsumed[evals[e].fromGroup]
\* fails at depth 27. EvalBodyFinish consumes the copy after the parent
\* evaluator returns done, while the initiating callback still owes sync.
\* At MaxEvals = 2 these sweeps use an eager parent. The bound cannot also
\* fit an earlier external parent evaluation and a concurrent child caller.
\*
\* Re-break: replace ParentPartFinal(pair[1], pair[2]) in SweepCandidates
\* with TRUE. SweepStartsFinal fails at depth 24 in this configuration.
\* The child then demands a parent part whose body is still unconsumed.
\* The entry-time evidence prevents a later parent completion from hiding it.
\*
\* One-time continuation check (2026-09-04): resume the NoSweep trace's
\* state 24 with MaxEvals = 3, NoGroupNeeds, AllowRelease = TRUE, and
\* LazyCanFail = TRUE. Use LiveSpec without symmetry. The invariants below,
\* EvalEventuallyTerminal, and DeferredReleaseEventuallyCompletes passed at
\* 456,983 distinct states / 4m48s. This checks a cache attempt waiting on a
\* direct copy, including cancellation and release. The probe
\*   NoOverlap == \A r \in ResultIds, g \in LazyGroups :
\*       res[r].lazySweepOwner[g] = "none" \/ res[r].lazyTokenSession[g] = 0
\* failed on that continuation, so the waiting cache attempt is reachable.
SPECIFICATION Spec
SYMMETRY SymmCalls
CONSTANTS
    Sessions = {s1}
    s1 = s1
    Calls = {cA, cB}
    cA = cA
    cB = cB
    ClassOf <- DistinctClasses
    MaxInvocations = 2
    MaxResults = 2
    Handles = {}
    AllowRelease = FALSE
    ReleaseSessions <- AllSessions
    PersistableIntent = FALSE
    DrainOnRelease = FALSE
    AllowPruneCut = FALSE
    ModelLazy = TRUE
    MaxEvals = 2
    LazyGroups <- CopyGroups
    LazyParts <- ContainerPartsTwo
    PartGroupOf <- CopyPartGroupOf
    GroupNeeds <- CopyGroupNeeds
    ModelContainerPartPersistence = FALSE
    ModelPartDelegation = TRUE
    ModelPersistence = FALSE
    ImportInit = FALSE
    ModelNestedCalls = FALSE
    ModelLateDeps = FALSE
    FnCanFail = FALSE
    AttachCanFail = FALSE
    LeaseCanFail = FALSE
    ReaderCanCancel = FALSE
    LazyCanFail = FALSE
    DecodeCanFail = FALSE
INVARIANTS TypeOK OwnershipExact LazyAttemptDefersCollection EvalDoneComplete LazyCompleteSettled LazyMutualExclusion LazySuccessPermanent NoStaleCancelError PartServedByItsGroup GroupOrderRespected SweepOwnerActive SweepStartsFinal
