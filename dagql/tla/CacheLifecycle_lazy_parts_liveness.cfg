\* Per-part evaluation under fair scheduling, prerequisite ordering
\* active: every Evaluate caller eventually terminates, including callers
\* that must settle gMeta through the resolution phase before their gOut
\* demand, and callers that retry a foreign cancellation on either group.
\* The new wedge risks stage 2 introduces live exactly here: an evaluator
\* parked in resolution (curGroup = gMeta) must be driven back to demand
\* for its own group when gMeta completes. Callback failures injected.
\* Expected: pass.
SPECIFICATION LiveSpec
CONSTANTS
    Sessions = {s1}
    s1 = s1
    Calls = {cA}
    cA = cA
    ClassOf <- OneClass
    MaxInvocations = 1
    MaxResults = 1
    Handles = {}
    AllowRelease = FALSE
    ReleaseSessions <- AllSessions
    PersistableIntent = TRUE
    DrainOnRelease = FALSE
    AllowPruneCut = FALSE
    ModelLazy = TRUE
    MaxEvals = 2
    LazyGroups <- ContainerGroups
    LazyParts <- ContainerParts
    PartGroupOf <- ContainerPartGroupOf
    GroupNeeds <- ContainerGroupNeeds
    ModelPartDelegation = FALSE
    ModelPersistence = FALSE
    ImportInit = FALSE
    ModelNestedCalls = FALSE
    ModelLateDeps = FALSE
    FnCanFail = FALSE
    AttachCanFail = FALSE
    LeaseCanFail = FALSE
    ReaderCanCancel = FALSE
    LazyCanFail = TRUE
    DecodeCanFail = FALSE
PROPERTY EvalEventuallyTerminal
