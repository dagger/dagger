\* remote_parts: one receiver, two parts, two demands, one producer and two
\* competing byte routes for fs; drain and final scan, fs acquired beside a
\* privately produced meta, cancel, join and the served-output postcondition.
\* Expected: pass.
CONSTANTS
    Tasks = {1, 2}
    MaxCancels = 1
    MaxOffers = 2
    MaxSyncFailures = 1
    DonorCanLeave = TRUE
    Fault = "none"
SPECIFICATION Spec
INVARIANTS TypeOK ServedOutputIsComplete FinalOutputNeverOverwritten WriterExclusive OfferAfterSealCannotPublish HoldsHaveLiveOwners QuiescentIsClean OwedMeansInstalled NoProgressIsUnreachable NoProgressOnlyOnARepeat
