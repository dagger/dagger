\* remote_sharing: donor, encoded receiver lacking two parts, bystander; one cohort and its successor, two preparation slots, one foreground decode or install; all-prepare then commit, the member-release barrier, a stale revision, independent synchronization.
\* Expected: pass.
CONSTANTS
    MaxForeground = 1
    MaxSyncFailures = 1
    StartDecoded = FALSE
    Fault = "none"
SPECIFICATION Spec
INVARIANTS TypeOK EveryHoldHasALiveOwner MembersAreLive MembersReleasedBeforeFinish DesiredCoversInstalled FinalOutputNeverOverwritten PinsBalanced DecodedReceiverOneSlotPerPass
