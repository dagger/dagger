\* remote_sharing_decoded: the same with a receiver that is already decoded: one slot per pass and the successor's continuous donor hold.
\* Expected: pass.
CONSTANTS
    MaxForeground = 1
    MaxSyncFailures = 1
    StartDecoded = TRUE
    Fault = "none"
SPECIFICATION Spec
INVARIANTS TypeOK EveryHoldHasALiveOwner MembersAreLive MembersReleasedBeforeFinish DesiredCoversInstalled FinalOutputNeverOverwritten PinsBalanced DecodedReceiverOneSlotPerPass
