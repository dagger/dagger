\* Deliberate fault: a decoded receiver's successor takes its holds after the members were released, so the donor can be collected in between.
\* Expected: MembersAreLive violated.
CONSTANTS
    MaxForeground = 1
    MaxSyncFailures = 1
    StartDecoded = TRUE
    Fault = "DecrementBeforeSuccessor"
SPECIFICATION Spec
INVARIANTS TypeOK MembersAreLive
