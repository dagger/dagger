\* A locally built chain starts without reuse indexes. Export selects existing
\* or generated bytes and retains resources until its provider is consumed.
\* One continuing owner starts with actual ancestry and receives newly recorded
\* present content. Expected: pass. 2026-09-05: 5,185,181 distinct states,
\* 17,628,508 generated, 24.80 seconds; Java 21, 8 GiB heap, 16 workers.
\* README records commands, source hashes, named probes and deliberate breaks.
CONSTANTS
    MaxStarts = 2
    MaxSnapshots = 3
    LocalBuild = TRUE
    SourceCanFail = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK ApplyExclusive ReuseBeforeBytes RetainedAncestry OwnerHasRecordedContent ApplyHasBytes ProviderHasResources CleanupComplete
