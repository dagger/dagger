\* Two importers, a shared prefix/suffix, two owners, source failure and GC.
\* Three physical creations permit a successful suffix to be pruned and applied again.
\* Expected: pass. 2026-09-05: 475,119 distinct states, 1,295,189 generated,
\* 4.51 seconds; Java 21, 8 GiB heap, 16 workers. README records commands,
\* source hashes, named reachability probes and deliberate invariant breaks.
CONSTANTS
    MaxStarts = 3
    MaxSnapshots = 3
    LocalBuild = FALSE
    SourceCanFail = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK ApplyExclusive ReuseBeforeBytes RetainedAncestry OwnerHasRecordedContent ApplyHasBytes ProviderHasResources CleanupComplete
