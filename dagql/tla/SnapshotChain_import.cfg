\* Two importers, a shared prefix/suffix, two owners, source failure and GC.
\* Three physical creations permit a successful suffix to be pruned and applied again.
CONSTANTS
    MaxStarts = 3
    MaxSnapshots = 3
    LocalBuild = FALSE
    SourceCanFail = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK ApplyExclusive ReuseBeforeBytes RetainedAncestry ApplyHasBytes ProviderHasResources CleanupComplete
