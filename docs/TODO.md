# Docs TODO

Deferred work that is intentionally not blocking the current restructure.

## Reference

- **Workspace Configuration reference page** — `.dagger/config.toml` schema. Every persona in the focus group flagged this as missing. Linked from `adopting/workspace-setup.mdx` and `reference/upgrade-to-workspaces.mdx` implicitly; both would benefit from a real schema page to point at. Should cover top-level keys, `[modules.*]`, `[modules.*.config]`, `entrypoint = true`, and precedence rules with `.env` / constructor defaults.
