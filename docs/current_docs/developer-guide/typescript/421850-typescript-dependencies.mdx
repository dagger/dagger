---
slug: /developer-guide/typescript/421860/typescript-dependencies
displayed_sidebar: 'zenith'
toc_max_heading_level: 2
title: 'TypeScript Dependencies'
---


# TypeScript Dependencies


To add a TypeScript dependency, simply add it to the `package.json`'s `dependencies` using your favorite package manager:

```shell
npm install pm2
```

Pinning a specific dependency version or adding local dependencies are supported, in the same way as any Node.js project.
