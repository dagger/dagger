---
title: "Developing Modules — TypeScript"
slug: /extending/editions/typescript
sidebar_label: "TypeScript Edition"
description: "Learn to develop Dagger modules using TypeScript"
---

# Developing Modules — TypeScript Edition

See the [Base Edition](/extending/editions/dang) for the complete guide — the patterns are the same across all SDKs.

## Recipes

### Builds

#### Perform a multi-stage build

The following Dagger Function performs a multi-stage build.

```typescript file=./snippets/builds/multi-stage-build/index.ts
```

#### Perform a matrix build

The following Dagger Function performs a matrix build.

```typescript file=./snippets/builds/matrix-build/index.ts
```

#### Build image from Dockerfile

The following Dagger Function builds an image from a Dockerfile.

```typescript file=./snippets/builds/dockerfile/index.ts
```

#### Build image from Dockerfile using different build context

The following Dagger Function builds an image from a Dockerfile with a build context that is different than the current working directory.

```typescript file=./snippets/builds/dockerfile-context/index.ts
```

#### Build multi-arch image

The following Dagger Function builds a single image for different CPU architectures using native emulation.

```typescript file=./snippets/builds/multi-arch/index.ts
```

#### Build multi-arch image with cross-compilation

The following Dagger Function builds a single image for different CPU architectures using cross-compilation.

```typescript file=./snippets/builds/multi-arch-cc/index.ts
```

#### Add OCI labels to image

The following Dagger Function adds OpenContainer Initiative (OCI) labels to an image.

```typescript file=./snippets/builds/oci-labels/index.ts
```

#### Add OCI annotations to image

The following Dagger Function adds OpenContainer Initiative (OCI) annotations to an image.

```typescript file=./snippets/builds/oci-annotations/index.ts
```

#### Invalidate cache

The following Dagger Function demonstrates how to invalidate the Dagger layer cache by introducing a volatile time variable at a specific point in the workflow.

```typescript file=./snippets/builds/cache/index.ts
```

#### Cache application dependencies

The following Dagger Function uses a cache volume for application dependencies, enabling Dagger to reuse the contents of the cache across runs.

```typescript file=./snippets/cache-dependencies/index.ts
```

#### Execute functions concurrently

The following Dagger Function demonstrates how to use promises to execute other Dagger Functions concurrently.

```typescript file=./snippets/execute-concurrently/index.ts
```

#### Persist service state across runs

The following Dagger Function uses a cache volume to persist a Redis service's data across Dagger Function runs.

```typescript file=./snippets/services/persist-service-state/index.ts
```

### Container Images

#### Export a container image to the host

The following Dagger Function returns a just-in-time container that can be exported to the host as an OCI tarball.

```typescript file=./snippets/export-container/index.ts
```

#### Publish a container image to a private registry

The following Dagger Function publishes a just-in-time container image to a private registry.

```typescript file=./snippets/publish-image/index.ts
```

#### Publish a container image to a private registry with multiple tags

The following Dagger Function tags a just-in-time container image multiple times and publishes it to a private registry.

```typescript file=./snippets/tag-publish-image/index.ts
```

#### Set environment variables in a container

The following Dagger Function demonstrates how to set a single environment variable in a container.

```typescript file=./snippets/set-env-var/index.ts
```

The following Dagger Function demonstrates how to set multiple environment variables in a container.

```typescript file=./snippets/set-env-vars/index.ts
```

### Filesystems

#### Mount or copy a directory or remote repository to a container

The following Dagger Function accepts a `Directory` argument, mounts the specified directory to the `/src` path in a container, and returns the modified container.

```typescript file=./snippets/mount-dir/index.ts
```

An alternative option is to copy the target directory in the container.

```typescript file=./snippets/copy-dir/index.ts
```

#### Mount or copy a local or remote file to a container

The following Dagger Function accepts a `File` argument, mounts the specified file to a container in the `/src/` directory, and returns the modified container.

```typescript file=./snippets/mount-file/index.ts
```

An alternative option is to copy the target file to the container.

```typescript file=./snippets/copy-file/index.ts
```

#### Copy a file to the Dagger module runtime container for custom processing

The following Dagger Function accepts a `File` argument and copies the specified file to the Dagger module runtime container for custom processing.

```typescript file=./snippets/copy-file-runtime/index.ts
```

#### Copy a subset of a directory using pre-defined filters

The following Dagger Function copies a directory to the `/src` path in a container, using pre-defined filter patterns to exclude specified sub-directories and files.

```typescript file=./snippets/copy-filter-dir-pre/index.ts
```

#### Copy a subset of a directory using filters specified at run-time

The following Dagger Function copies a directory to the `/src` path in a container, using a filter pattern specified at call-time to exclude specified sub-directories and files.

```typescript file=./snippets/copy-filter-dir-post/index.ts
```

#### Modify a copied directory or remote repository in a container

The following Dagger Function copies a directory to the `/src` path in a container, adds a file to it, and returns the modified container.

```typescript file=./snippets/copy-modify-dir/index.ts
```

#### Export a directory or file to the host

The following Dagger Function returns a just-in-time directory and file that can be exported to the host.

```typescript file=./snippets/export-file-dir/index.ts
```

#### Clone a remote Git repository into a container

The following Dagger Function accepts a Git repository URL and a Git reference, copies the repository to the `/src` path in a container, and returns the modified container.

```typescript file=./snippets/clone-git-repository/index.ts
```

Clone over SSH with socket forwarding:

```typescript file=./snippets/clone-git-repository-ssh/index.ts
```

#### Request a file over HTTP/HTTPS and save it in a container

The following Dagger Function fetches a file over HTTP/HTTPS and saves it in a container.

```typescript file=./snippets/read-file-http/index.ts
```

#### Set a module-wide default path

The following Dagger module uses a constructor to set a module-wide `Directory` argument and point it to a default path on the host.

```typescript file=./snippets/set-common-default-path/index.ts
```

### Services

#### Bind and use services in Dagger Functions

The following Dagger Function creates and returns an HTTP service, which is bound and used from a different Dagger Function via a service binding.

```typescript file=./snippets/services/bind-services/index.ts
```

#### Expose services in Dagger Functions to the host

The following Dagger Function creates and returns an HTTP service that can be used from the host.

```typescript file=./snippets/services/expose-dagger-services-to-host/index.ts
```

#### Expose host services to Dagger Functions

The following Dagger Function accepts a `Service` running on the host, binds it using an alias, and creates a client to access it via the service binding.

```typescript file=./snippets/services/expose-host-services-to-dagger/index.ts
```

#### Start and stop services

The following Dagger Function demonstrates how to control a service's lifecycle by explicitly starting and stopping a Redis service.

```typescript file=./snippets/services/start-stop-services/index.ts
```

#### Use service endpoints

The following Dagger Function starts a service manually, retrieves its endpoint, and sends a request.

```typescript file=./snippets/services/use-endpoint/index.ts
```

#### Create interdependent services

The following Dagger Function runs two services that depend on each other, set up with custom hostnames for inter-service communication.

```typescript file=./snippets/services/create-interdependent-services/index.ts
```

#### Create a transient service for unit tests

The following Dagger Function creates a database service on-the-fly and binds it to an application container for unit testing.

```typescript file=./snippets/services/test-against-db-service/index.ts
```

### Secrets

#### Use secret variables

The following Dagger Function accepts a GitHub personal access token as a `Secret` and uses it to authorize a request to the GitHub API.

```typescript file=./snippets/secret-variable/index.ts
```

#### Mount files as secrets

The following Dagger Function accepts a GitHub hosts configuration file as a `Secret` and mounts it to a container to authorize a request to GitHub.

```typescript file=./snippets/secret-file/index.ts
```

#### Use secret in Dockerfile build

The following Dagger Function demonstrates how to inject a secret into a Dockerfile build, automatically mounted at `/run/secrets/SECRET-ID`.

```typescript file=./snippets/secret-dockerfile/index.ts
```

### Errors & Debugging

#### Terminate gracefully

The following Dagger Function demonstrates how to handle errors in a workflow.

```typescript file=./snippets/handle-errors/index.ts
```

#### Continue using a container after command execution fails

The following Dagger Function demonstrates how to continue using a container after a command executed within it fails.

```typescript file=./snippets/continue-after-errors/src/index.ts
```

#### Debug workflows with the interactive terminal

The following Dagger Function opens an interactive terminal session at different stages in a Dagger workflow to debug a container build.

```typescript file=./snippets/terminal-container/index.ts
```

#### Inspect directories and files

The following Dagger Function clones a GitHub repository and opens an interactive terminal session to inspect it.

```typescript file=./snippets/terminal-directory-1/index.ts
```

The container used to mount the directory can be customized. The following example uses an `ubuntu` container image and `bash` shell.

```typescript file=./snippets/terminal-directory-2/index.ts
```

#### Create custom spans

The following Dagger Function demonstrates how to emit custom OpenTelemetry spans for various tasks.

```typescript file=./snippets/custom-spans/index.ts
```

## IDE setup

When opening the generated `dagger/src/index.ts` in an IDE, most IDEs will automatically recognize the `@dagger.io/dagger` package, so long as the `tsconfig.json` file has a path configured on it.

For Dagger modules initialized using `dagger module init`, the default template is already configured this way:

```json
    "experimentalDecorators": true,
    "paths": {
      "@dagger.io/dagger": ["./sdk"]
    }
```

This configuration doesn't require separate installation of the dependency to enable type-hinting or other IDE features.

### Source maps

When working with multiple inter-dependent Dagger modules, it can be challenging to navigate between Dagger Functions defined in different modules. Source-maps offer a solution to this problem, by allowing developers to directly click through or navigate to type declarations in their IDE.

The TypeScript SDK automatically attaches source-maps to the module code. These are a record of the filename, the line number, and the column number that a type declaration corresponds to in the source code. The SDK analyzes the source code, and outputs these (as line comments of the form `./path/to/filename:line`) when creating the type definitions during module initialization.

Most popular IDEs support source-maps, either natively or with an external plugin, such as the [Open file plugin](https://marketplace.visualstudio.com/items?itemName=Fr43nk.seito-openfile) for Visual Studio Code.
