---
title: "Pre-push workflow"
slug: /getting-started/pre-push
description: "Run your checks locally, before you push"
---

# Pre-push workflow

How to use Dagger before a commit leaves your machine: run the same checks, generators, and services locally that CI will run after the push, so failures surface in seconds instead of minutes.
