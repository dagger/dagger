---
title: "Helm"
slug: /modules/helm
description: "Lint Helm charts and check rendered templates."
---

# Helm

Official module: [dagger/helm](https://github.com/dagger/helm)

Install with `dagger mod install github.com/dagger/helm`.

## What It Is

The Helm module validates Helm charts.

It discovers charts in the workspace, lints them, and checks that values files render with `helm template --dry-run=client`.

## Use It For

- Check chart structure with Helm lint.
- Check that values files render cleanly.
- Catch chart problems before a PR reaches a cluster.
- Share the same Helm version across a workspace.

## Main Workflows

| Workflow | Function | Use it for |
| --- | --- |
| Check | `lint` | Run Helm lint on every discovered chart. |
| Check | `assert-template` | Render discovered values files and fail if templating breaks. |
| Discover | `charts` | See which charts the module finds. |

## How to Use It

Start with `lint`. Add `assert-template` when values files are important to the release path.

Use chart discovery helpers when a workflow needs to operate on one chart or explain what the module found.

## Workspace Fit

Use this module for repos that ship Kubernetes applications with Helm. It is a strong PR check because it validates chart changes before deploy tooling sees them.

## Source

Source repo: [github.com/dagger/helm](https://github.com/dagger/helm)
