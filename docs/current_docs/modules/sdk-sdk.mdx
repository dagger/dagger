---
title: "SDK SDK"
slug: /modules/sdk-sdk
description: "Skeleton docs for the official SDK SDK module."
---

# SDK SDK

Official module: [dagger/sdk-sdk](https://github.com/dagger/sdk-sdk)

Install with `dagger mod install github.com/dagger/sdk-sdk`.

## What It Is

SDK SDK is an advanced module for developing custom Dagger SDKs.

The module did not load successfully against the current 1.0 beta CLI during this docs pass because one dependency failed Dang type inference. Keep this page conservative until the module API can be inspected cleanly.

## Use It For

- Building a custom SDK
- Generating SDK support files
- Testing SDK behavior
- Understanding expert-level Dagger extension points

## Expected Workflows

- Scaffold SDK pieces
- Generate bindings
- Run SDK checks
- Package or publish SDK outputs

## Functions

The current public function list is not available from this environment. Re-run inspection once the dependency load issue is fixed, then add the real functions here.

## Settings

Document target language, generator settings, source paths, and compatibility constraints once the API is available.

## Examples to Add

- Generate a minimal SDK
- Test generated bindings
- Compare with official SDK docs

## Source

Source repo: [github.com/dagger/sdk-sdk](https://github.com/dagger/sdk-sdk)
