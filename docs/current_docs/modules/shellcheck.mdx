---
title: "ShellCheck"
slug: /modules/shellcheck
description: "Check shell scripts with ShellCheck."
---

# ShellCheck

Official module: [dagger/shellcheck](https://github.com/dagger/shellcheck)

Install with `dagger mod install github.com/dagger/shellcheck`.

## What It Is

The ShellCheck module finds shell scripts and checks them with ShellCheck.

Use it when scripts are part of the project and should be treated like code, not as untested glue.

## Use It For

- Check shell scripts in CI and Dagger Cloud.
- Catch quoting, portability, and safety issues.
- Exclude known files when a workspace needs a narrow check.

## Main Workflows

| Workflow | Function | Use it for |
| --- | --- |
| Check | `check` | Run ShellCheck on discovered shell scripts. |
| Discover | `scripts` | See which `.sh` files the module finds. |

## How to Use It

Start with `check`. If the result is surprising, use `scripts` to inspect discovery.

Use `exclude` only for files that should not be checked. Keep the default path broad so new scripts are checked automatically.

## Workspace Fit

This module is small and high value. It is a good default check for repos that contain deployment scripts, local dev scripts, or CI helper scripts.

## Source

Source repo: [github.com/dagger/shellcheck](https://github.com/dagger/shellcheck)
