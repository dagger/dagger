
### Forward service ports to localhost

Use `withLocalhostForward` to make a service reachable on `127.0.0.1` inside a container, instead of a custom hostname. This is useful when your application or test framework expects services on `localhost`.

<Tabs groupId="language" queryString="sdk">
<TabItem value="go" label="Go">

```go file=../../../extending/modules/snippets/services/localhost-forward/go/main.go
```

</TabItem>
<TabItem value="python" label="Python">

```python file=../../../extending/modules/snippets/services/localhost-forward/python/main.py
```

</TabItem>
<TabItem value="typescript" label="TypeScript">

```typescript file=../../../extending/modules/snippets/services/localhost-forward/typescript/index.ts
```

</TabItem>
</Tabs>

#### Example

Send a request from a Dagger Function to an HTTP service forwarded to localhost:

<Tabs groupId="shell">
<TabItem value="System shell">
```shell
dagger -c get
```
</TabItem>
<TabItem value="Dagger Shell">
```shell title="First type 'dagger' for interactive mode."
get
```
</TabItem>
<TabItem value="Dagger CLI">
```shell
dagger call get
```
</TabItem>
</Tabs>
