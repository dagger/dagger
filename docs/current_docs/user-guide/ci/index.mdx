---
slug: /user-guide/ci
---

# Dagger in CI

This section provides information and code samples to help you integrate Dagger with popular CI providers/tools.

- GitHub Actions


## GitLab CI

```yaml title=".gitlab-ci.yml" file=./snippets/gitlab.yml
```

## CircleCI

```yaml title=".circleci/config.yml" file=./snippets/circle.yml
```

## Jenkins

Requires `docker` client installed on your Jenkins agent, a Docker host available (can be `docker:dind`), and agents labeled in Jenkins with `dagger`.

```groovy title="Jenkinsfile" file=./snippets/Jenkinsfile
```

## Azure Pipelines

```yaml title="azure-pipelines.yml" file=./snippets/azure-pipelines.yml
```

## AWS CodePipeline

```yaml title="buildspec.yml" file=./snippets/buildspec.yml
```
