---
id: overview
title: Overview
displayed_sidebar: "0.2"
slug: /
---

import styles from "@site/src/css/overview.module.scss";
import Link from "@docusaurus/Link";
import Button from "@site/src/components/atoms/button.js";
import TutorialCard from "@site/src/components/molecules/tutorialCard.js";
import LinkCTA from "@site/src/components/atoms/linkCTA.js";
import { useDocsSidebar } from "@docusaurus/theme-common";

# Dagger CUE SDK

### What is the Dagger CUE SDK?

The Dagger CUE SDK contains everything you need to develop CI/CD pipelines in CUE, and run them on any OCI-compatible container runtime.

Currently the CUE SDK consists of:

- A CLI, `dagger-cue`
- The Dagger Engine, which is currently laucned by the CLI.
- CUE SDK packages and sample applications
- This documentation

### Who is it for?

### How it works

### Getting started
To learn more, install the CUE SDK and start using it.
