---
title: "Generating Code"
slug: /using-dagger/generating
description: "Run code generators and review changesets before applying them"
---

# Generating Code

```shell
dagger generate
```

Runs all generators in your workspace. Each generator produces a [changeset](/1.0-beta/using-dagger/changesets) — a diff you review before it touches your working tree.

## List generators

```shell
dagger generate -l
```

## Filter generators

```shell
dagger generate protobuf:*         # all generators from a module
dagger generate changelog:generate  # a single generator
```

## Verify in CI

Run the generators as checks. This fails the build if any generated file is out of date, without modifying your tree:

```yaml
# GitHub Actions
- run: dagger check --generate
```

See [Changesets](/1.0-beta/using-dagger/changesets) for how reviewing and applying generated changes works locally.
