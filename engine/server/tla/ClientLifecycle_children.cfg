\* One accepted parent request may create multiple distinct child clients.
\* Every client ID owns an independent one-shot transport and child lease;
\* closing either must not close, resurrect, or substitute the other.
\* Expected: pass.
SPECIFICATION LiveSpec
CONSTANTS
    Clients = {root, first, second}
    root = root
    first = first
    second = second
    Root = root
    NoClient = none
    ParentOf <- RootParents
    Requests = {q1, q2, q3}
    Works = {}
    ModelBackground = FALSE
    ModelChildren = TRUE
    ModelTeardown = FALSE
INVARIANTS TypeOK LeaseExact ActiveWorkHasRuntime NoEarlyReclamation MetricsDrainAfterProducers MetricsStopBeforeReclamation NoClientResurrection ChildRetainsParent
PROPERTY QuiescentClientEventuallyReclaimed
