\* Request admission, transport close, last-request release, and reclamation
\* can interleave in every order. Expected: pass.
SPECIFICATION LiveSpec
CONSTANTS
    Clients = {root}
    root = root
    Root = root
    NoClient = none
    ParentOf <- RootParents
    Requests = {q1, q2}
    Works = {}
    ModelBackground = FALSE
    ModelChildren = FALSE
    ModelTeardown = FALSE
INVARIANTS TypeOK LeaseExact ActiveWorkHasRuntime NoEarlyReclamation MetricsDrainAfterProducers MetricsStopBeforeReclamation NoClientResurrection
PROPERTY QuiescentClientEventuallyReclaimed
