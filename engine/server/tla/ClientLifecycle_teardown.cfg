\* Session teardown races transport closure and detached shared work. Cache
\* release may return deferred, but telemetry cannot stop until the callback
\* exits and per-session cache cleanup completes. Expected: pass.
SPECIFICATION LiveSpec
CONSTANTS
    Clients = {root, child}
    root = root
    child = child
    Root = root
    NoClient = none
    ParentOf <- RootParents
    Requests = {q1}
    Works = {w1}
    ModelBackground = TRUE
    ModelChildren = TRUE
    ModelTeardown = TRUE
    BlockingRegistration = FALSE
INVARIANTS TypeOK LeaseExact ActiveWorkHasRuntime NoEarlyReclamation MetricsDrainAfterProducers MetricsStopBeforeReclamation NoClientResurrection ChildRetainsParent TelemetryStopsAfterProducers
PROPERTIES QuiescentClientEventuallyReclaimed QuiescentTeardownEventuallyCompletes TeardownEventuallyCompletes
