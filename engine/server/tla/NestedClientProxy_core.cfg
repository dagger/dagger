\* Exact nested routing with distinct scope and metadata identities, multiple
\* logical clients, one-shot transports, and process cleanup. Expected: pass.
SPECIFICATION Spec
CONSTANTS
    Clients = {base, first, second}
    Base = base
    NoClient = none
    ScopeParent = executable
    MetadataParent = workspaceOwner
    ForeignCarrier = foreignCarrier
    ExecSecret = execSecret
    OtherSecret = otherSecret
    Requests = {q1, q2, q3}
    UseMetadataParent = FALSE
    AllowCrossCarrier = FALSE
    AllowClosedRebind = FALSE
    AllowClosedSubstitution = FALSE
    AllowMalformedFallback = FALSE
    RetryClosedTransport = FALSE
    CloseAllOnOneClose = FALSE
INVARIANTS TypeOK ParentUsesScope CarrierBindingExact ExactRouting MalformedRejected NoClosedIDRebind ClosedIsTerminal IndependentClose ProxyCloseTerminal
