overwrite: true
schema: "./gen/alpine/alpine.graphql"
documents:
  - "./gen/alpine/operations.graphql"
generates:
  ./gen/alpine/alpine.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
