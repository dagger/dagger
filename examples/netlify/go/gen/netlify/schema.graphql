scalar FS


type Deploy {
  url: String!
  deployUrl: String!
  logsUrl: String
}

""
type Netlify {
	deploy(contents: FS!, subdir: String, siteName: String, token: Secret!): Deploy!
}

type Query {
	""
	netlify: Netlify!
}
	