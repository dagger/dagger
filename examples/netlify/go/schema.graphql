scalar FS
scalar Secret

type Deploy {
  url: String!
  deployUrl: String!
  logsUrl: String
}

type Query {
  deploy(
    contents: FS!
    subdir: String
    siteName: String
    token: Secret!
  ): Deploy!
}
