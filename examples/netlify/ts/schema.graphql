extend type Query {
  netlify: Netlify!
}

type Netlify {
  deploy(
    contents: FSID!
    subdir: String
    siteName: String
    token: SecretID!
  ): Deploy!
}

type Deploy {
  url: String!
  deployUrl: String!
  logsUrl: String
}
