actions:
   alpine:
     local: ../..
     dockerfile: examples/alpine/Dockerfile
  # yarn:
  #   local: ../..
  #   dockerfile: examples/yarn/Dockerfile
  # netlify:
  #   local: ../..
  #   dockerfile: examples/netlify/ts/Dockerfile
  # # Registry
  # alpine:
  #   image: localhost:5555/dagger:alpine
  # # Git (not yet supported)
  # netlify:
  #   git: https://github.com/dagger/cloak.git
  #   dockerfile: examples/netlify/Dockerfile
