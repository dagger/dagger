dependencies:
  - local: examples/alpine
  - local: examples/yarn
  - local: examples/netlify/ts
  # Registry (not yet supported)
  # - image: localhost:5555/dagger:alpine
  # # Git (not yet supported)
  # - git: https://github.com/dagger/cloak.git
  # # Dockerfile (not yet supported)
  # - dockerfile: examples/netlify/Dockerfile
