{
  source {
    git(remote: "github.com/moby/buildkit") {
      dockerfile {
        exec(args: ["sh", "-c", "buildctl --help > /out.txt"]) {
          file(path: "/out.txt")
        }
      }
    }
  }
}
