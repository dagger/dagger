{
  source {
    git(remote: "github.com/moby/buildkit", ref: "v0.10.3") {
      dockerfile {
        exec(args: ["sh", "-c", "buildctl --help > /out.txt"]) {
          file(path: "/out.txt")
        }
      }
    }
  }
}
