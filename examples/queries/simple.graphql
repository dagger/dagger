{
  source {
    image(ref: "alpine") {
      exec(args: ["apk", "add", "curl"]) {
        fs {
          exec(args: ["curl", "https://github.com/", "-o", "/tmp/foo"]) {
            fs {
              file(path: "/tmp/foo")
            }
          }
        }
      }
    }
  }
}
