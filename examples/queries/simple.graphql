{
  source {
    image(ref: "alpine") {
      exec(args: ["apk", "add", "curl"]) {
        fs {
          exec(args: ["curl", "https://dagger.io/"]) {
            stdout(lines: 1)
          }
        }
      }
    }
  }
}
