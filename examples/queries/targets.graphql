query build {
  source {
    git(remote: "github.com/dagger/dagger") {
      dockerbuild {
        id
      }
    }
  }
}

query test {
  source {
    git(remote: "github.com/dagger/dagger") {
      file(path: "README.md")
    }
  }
}
