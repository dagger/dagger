scalar FS


type Deploy {
  url: String!
  deployUrl: String!
  logsUrl: String
}
type Netlify {
	deploy(contents: FS!, subdir: String, siteName: String, token: String): Deploy!
}
type Query {
	netlify: Netlify!
}
	