query Build($src: FS!) {
  todoapp {
    build(src: $src)
  }
}

query Test($src: FS!) {
  todoapp {
    test(src: $src)
  }
}

query Deploy($src: FS!, $token: Secret!) {
  todoapp {
    deploy(src: $src, token: $token) {
      url
      deployUrl
    }
  }
}
