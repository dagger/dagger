extend type Query {
  todoapp: Todoapp!
}

type Todoapp {
  build(src: FSID!): Filesystem!
  test(src: FSID!): Filesystem!
  deploy(src: FSID!, token: SecretID!): DeployURLs!
}

type DeployURLs {
  url: String!
  deployURL: String!
  logsURL: String
}
