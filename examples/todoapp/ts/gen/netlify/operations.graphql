query Deploy(
  $contents: FS!
  $subdir: String
  $siteName: String
  $token: Secret!
) {
  netlify {
    deploy(
      contents: $contents
      subdir: $subdir
      siteName: $siteName
      token: $token
    ) {
      url
      deployUrl
    }
  }
}
