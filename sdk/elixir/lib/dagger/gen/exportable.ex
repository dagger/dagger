# This file generated by `dagger_codegen`. Please DO NOT EDIT.
defmodule Dagger.Exportable do
  @moduledoc """
  An object that can be exported to the host.

  Calling export writes the object to a path on the host filesystem and returns the path that was written.
  """

  use Dagger.Core.Base, kind: :interface, name: "Exportable"

  alias Dagger.Core.Client
  alias Dagger.Core.QueryBuilder, as: QB

  @derive Dagger.ID

  defstruct [:query_builder, :client]

  @type t() :: %__MODULE__{}

  @spec export(t(), String.t()) :: {:ok, String.t()} | {:error, term()}
  def export(%__MODULE__{} = exportable, path) do
    query_builder =
      exportable.query_builder |> QB.select("export") |> QB.put_arg("path", path)

    Client.execute(exportable.client, query_builder)
  end

  @spec id(t()) :: {:ok, String.t()} | {:error, term()}
  def id(%__MODULE__{} = exportable) do
    query_builder =
      exportable.query_builder |> QB.select("id")

    Client.execute(exportable.client, query_builder)
  end
end

defimpl Jason.Encoder, for: Dagger.Exportable do
  def encode(exportable, opts) do
    {:ok, id} = Dagger.Exportable.id(exportable)
    Jason.Encode.string(id, opts)
  end
end

defimpl Nestru.Decoder, for: Dagger.Exportable do
  def decode_fields_hint(_struct, _context, id) do
    alias Dagger.Core.QueryBuilder, as: QB
    dag = Dagger.Global.dag()

    {:ok,
     %Dagger.Exportable{
       query_builder:
         dag.query_builder
         |> QB.select("node")
         |> QB.put_arg("id", id)
         |> QB.inline_fragment("Exportable"),
       client: dag.client
     }}
  end
end
