# This file generated by `dagger_codegen`. Please DO NOT EDIT.
defmodule Dagger.LLMMessageOrigin do
  @moduledoc """
  The recorded provenance of a message that arrived through an agent mailbox.
  """

  use Dagger.Core.Base, kind: :object, name: "LLMMessageOrigin"

  alias Dagger.Core.Client
  alias Dagger.Core.QueryBuilder, as: QB

  @derive Dagger.ID

  defstruct [:query_builder, :client]

  @type t() :: %__MODULE__{}

  @doc """
  The sending agent's instance ID (for AGENT origins) or the observed agent's instance ID (for EVENT origins).
  """
  @spec agent_id(t()) :: {:ok, String.t()} | {:error, term()}
  def agent_id(%__MODULE__{} = llm_message_origin) do
    query_builder =
      llm_message_origin.query_builder |> QB.select("agentId")

    Client.execute(llm_message_origin.client, query_builder)
  end

  @doc """
  The display name of the agent behind agentId.
  """
  @spec agent_name(t()) :: {:ok, String.t()} | {:error, term()}
  def agent_name(%__MODULE__{} = llm_message_origin) do
    query_builder =
      llm_message_origin.query_builder |> QB.select("agentName")

    Client.execute(llm_message_origin.client, query_builder)
  end

  @doc """
  A unique identifier for this LLMMessageOrigin.
  """
  @spec id(t()) :: {:ok, String.t()} | {:error, term()}
  def id(%__MODULE__{} = llm_message_origin) do
    query_builder =
      llm_message_origin.query_builder |> QB.select("id")

    Client.execute(llm_message_origin.client, query_builder)
  end

  @doc """
  Who put this message on the record.
  """
  @spec kind(t()) :: {:ok, Dagger.LLMMessageOriginKind.t()} | {:error, term()}
  def kind(%__MODULE__{} = llm_message_origin) do
    query_builder =
      llm_message_origin.query_builder |> QB.select("kind")

    case Client.execute(llm_message_origin.client, query_builder) do
      {:ok, enum} -> {:ok, Dagger.LLMMessageOriginKind.from_string(enum)}
      error -> error
    end
  end

  @doc """
  The message's short ref within the receiving agent's runtime, e.g. "#3": the deterministic token replies name (send's replyTo). Distinct from the AgentMessage handle's opaque message ID.
  """
  @spec ref(t()) :: {:ok, String.t()} | {:error, term()}
  def ref(%__MODULE__{} = llm_message_origin) do
    query_builder =
      llm_message_origin.query_builder |> QB.select("ref")

    Client.execute(llm_message_origin.client, query_builder)
  end

  @doc """
  The ref of the message this one answers, in the sender's own runtime, if any.
  """
  @spec reply_to(t()) :: {:ok, String.t()} | {:error, term()}
  def reply_to(%__MODULE__{} = llm_message_origin) do
    query_builder =
      llm_message_origin.query_builder |> QB.select("replyTo")

    Client.execute(llm_message_origin.client, query_builder)
  end
end

defimpl Jason.Encoder, for: Dagger.LLMMessageOrigin do
  def encode(llm_message_origin, opts) do
    {:ok, id} = Dagger.LLMMessageOrigin.id(llm_message_origin)
    Jason.Encode.string(id, opts)
  end
end

defimpl Nestru.Decoder, for: Dagger.LLMMessageOrigin do
  def decode_fields_hint(_struct, _context, id) do
    alias Dagger.Core.QueryBuilder, as: QB
    dag = Dagger.Global.dag()

    {:ok,
     %Dagger.LLMMessageOrigin{
       query_builder:
         dag.query_builder
         |> QB.select("node")
         |> QB.put_arg("id", id)
         |> QB.inline_fragment("LLMMessageOrigin"),
       client: dag.client
     }}
  end
end
