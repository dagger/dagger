# This file generated by `dagger_codegen`. Please DO NOT EDIT.
defmodule Dagger.ServiceDirectoryMount do
  @moduledoc """
  A directory mounted into a running service.
  """

  use Dagger.Core.Base, kind: :object, name: "ServiceDirectoryMount"

  alias Dagger.Core.Client
  alias Dagger.Core.QueryBuilder, as: QB

  @derive Dagger.ID
  @derive Dagger.Sync
  defstruct [:query_builder, :client]

  @type t() :: %__MODULE__{}

  @doc """
  Return changes from the original source, or from an explicit base Directory.
  """
  @spec changes(t(), [{:from, Dagger.Directory.t() | nil}]) :: Dagger.Changeset.t()
  def changes(%__MODULE__{} = service_directory_mount, optional_args \\ []) do
    query_builder =
      service_directory_mount.query_builder
      |> QB.select("changes")
      |> QB.maybe_put_arg(
        "from",
        if(optional_args[:from], do: Dagger.ID.id!(optional_args[:from]), else: nil)
      )

    %Dagger.Changeset{
      query_builder: query_builder,
      client: service_directory_mount.client
    }
  end

  @doc """
  Snapshot the current mount contents as an immutable Directory.

  By default, Dagger detaches the old mutable mount and remounts a fresh mutable copy of the snapshot so the service can keep using the same path.
  """
  @spec directory(t(), [{:keep_mounted, boolean() | nil}]) :: Dagger.Directory.t()
  def directory(%__MODULE__{} = service_directory_mount, optional_args \\ []) do
    query_builder =
      service_directory_mount.query_builder
      |> QB.select("directory")
      |> QB.maybe_put_arg("keepMounted", optional_args[:keep_mounted])

    %Dagger.Directory{
      query_builder: query_builder,
      client: service_directory_mount.client
    }
  end

  @doc """
  A unique identifier for this ServiceDirectoryMount.
  """
  @spec id(t()) :: {:ok, String.t()} | {:error, term()}
  def id(%__MODULE__{} = service_directory_mount) do
    query_builder =
      service_directory_mount.query_builder |> QB.select("id")

    Client.execute(service_directory_mount.client, query_builder)
  end

  @doc """
  Path where this directory is mounted in the service container.
  """
  @spec path(t()) :: {:ok, String.t()} | {:error, term()}
  def path(%__MODULE__{} = service_directory_mount) do
    query_builder =
      service_directory_mount.query_builder |> QB.select("path")

    Client.execute(service_directory_mount.client, query_builder)
  end

  @doc """
  The service this mount belongs to.
  """
  @spec service(t()) :: Dagger.Service.t()
  def service(%__MODULE__{} = service_directory_mount) do
    query_builder =
      service_directory_mount.query_builder |> QB.select("service")

    %Dagger.Service{
      query_builder: query_builder,
      client: service_directory_mount.client
    }
  end

  @doc """
  Force the runtime mount side effect.
  """
  @spec sync(t()) :: {:ok, Dagger.ServiceDirectoryMount.t()} | {:error, term()}
  def sync(%__MODULE__{} = service_directory_mount) do
    query_builder =
      service_directory_mount.query_builder |> QB.select("sync")

    with {:ok, id} <- Client.execute(service_directory_mount.client, query_builder) do
      {:ok,
       %Dagger.ServiceDirectoryMount{
         query_builder:
           QB.query()
           |> QB.select("node")
           |> QB.put_arg("id", id)
           |> QB.inline_fragment("ServiceDirectoryMount"),
         client: service_directory_mount.client
       }}
    end
  end

  @doc """
  Detach the mount from the service and release its mutable backing ref.
  """
  @spec unmount(t()) :: Dagger.Service.t()
  def unmount(%__MODULE__{} = service_directory_mount) do
    query_builder =
      service_directory_mount.query_builder |> QB.select("unmount")

    %Dagger.Service{
      query_builder: query_builder,
      client: service_directory_mount.client
    }
  end
end

defimpl Jason.Encoder, for: Dagger.ServiceDirectoryMount do
  def encode(service_directory_mount, opts) do
    {:ok, id} = Dagger.ServiceDirectoryMount.id(service_directory_mount)
    Jason.Encode.string(id, opts)
  end
end

defimpl Nestru.Decoder, for: Dagger.ServiceDirectoryMount do
  def decode_fields_hint(_struct, _context, id) do
    alias Dagger.Core.QueryBuilder, as: QB
    dag = Dagger.Global.dag()

    {:ok,
     %Dagger.ServiceDirectoryMount{
       query_builder:
         dag.query_builder
         |> QB.select("node")
         |> QB.put_arg("id", id)
         |> QB.inline_fragment("ServiceDirectoryMount"),
       client: dag.client
     }}
  end
end
