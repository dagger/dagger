// Currently a placeholder until we add Node Runtime
// eslint-disable-next-line @typescript-eslint/no-unused-vars
async function entrypoint() {
  // Get the directory path and list files in it to load
  // Make the switch statement to get parent name and know if it's a registry
  // or an invocation
  // If it's an invocation, parse and load state and input then call `invoke`
  // and format the return value and send it to the API
}
