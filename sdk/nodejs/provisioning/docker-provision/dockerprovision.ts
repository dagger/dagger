// trim image digests to 16 characters to make output more readable
const digestLen = 16;
const containerNamePrefix = "dagger-engine";
const engineSessionBinPrefix = "dagger-engine-session-";
const containerEngineSessionBinPrefix = "/usr/bin/" + engineSessionBinPrefix;

