[project]
name = "codegen"
version = "0.0.0"
description = "Codegen for the Python SDK"
requires-python = ">= 3.10"
dependencies = [
    "graphql-core>=3.2.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
