[project]
name = "demo"
version = "0.0.0"
dependencies = [
    "dagger-io[server] @ {root:uri}/../.."
]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
