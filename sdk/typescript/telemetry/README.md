# @dagger.io/telemetry

The Dagger OpenTelemetry for TypeScript instrumentation on dagger module.

This library expose a configured OpenTelemetry client to traces dagger workflows.

## Installation

```
npm install @dagger.io/telemetry
```
