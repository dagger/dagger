// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

type CliDev struct { // cli-dev (../../../../:0:0)
	query *querybuilder.Selection

	id              *ID
	publishMetadata *Void
	releaseDryRun   *Void
	tag             *string
	version         *string
}

func (r *CliDev) WithGraphQLQuery(q *querybuilder.Selection) *CliDev {
	return &CliDev{
		query: q,
	}
}

// CliDevBinaryOpts contains options for CliDev.Binary
type CliDevBinaryOpts struct {
	Platform Platform
}

// Build the dagger CLI binary for a single platform
func (r *CliDev) Binary(opts ...CliDevBinaryOpts) *File {
	q := r.query.Select("binary")
	for i := len(opts) - 1; i >= 0; i-- {
		// `platform` optional argument
		if !querybuilder.IsZeroValue(opts[i].Platform) {
			q = q.Arg("platform", opts[i].Platform)
		}
	}

	return &File{
		query: q,
	}
}

// CliDevDevBinariesOpts contains options for CliDev.DevBinaries
type CliDevDevBinariesOpts struct {
	Platform Platform
}

// Build dev CLI binaries
// TODO: remove this
func (r *CliDev) DevBinaries(opts ...CliDevDevBinariesOpts) *Directory {
	q := r.query.Select("devBinaries")
	for i := len(opts) - 1; i >= 0; i-- {
		// `platform` optional argument
		if !querybuilder.IsZeroValue(opts[i].Platform) {
			q = q.Arg("platform", opts[i].Platform)
		}
	}

	return &Directory{
		query: q,
	}
}

// A unique identifier for this CliDev.
func (r *CliDev) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *CliDev) XXX_GraphQLType() string {
	return "CliDev"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *CliDev) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *CliDev) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *CliDev) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *CliDev) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = CliDev{query: selectNode(dag.query, id, "CliDev")}
	return nil
}

// CliDevPublishOpts contains options for CliDev.Publish
type CliDevPublishOpts struct {
	GithubToken *Secret

	GithubHost string

	GithubCaCert *File

	AwsAccessKeyID *Secret

	AwsSecretAccessKey *Secret

	AwsRegion string

	AwsBucket string

	ArtefactsFqdn string

	AwsEndpointURL string

	DryRun bool
}

// Publish the CLI release artifacts.
func (r *CliDev) Publish(tag string, commit string, githubOrgName string, opts ...CliDevPublishOpts) *Directory {
	q := r.query.Select("publish")
	for i := len(opts) - 1; i >= 0; i-- {
		// `githubToken` optional argument
		if !querybuilder.IsZeroValue(opts[i].GithubToken) {
			q = q.Arg("githubToken", opts[i].GithubToken)
		}
		// `githubHost` optional argument
		if !querybuilder.IsZeroValue(opts[i].GithubHost) {
			q = q.Arg("githubHost", opts[i].GithubHost)
		}
		// `githubCaCert` optional argument
		if !querybuilder.IsZeroValue(opts[i].GithubCaCert) {
			q = q.Arg("githubCaCert", opts[i].GithubCaCert)
		}
		// `awsAccessKeyId` optional argument
		if !querybuilder.IsZeroValue(opts[i].AwsAccessKeyID) {
			q = q.Arg("awsAccessKeyId", opts[i].AwsAccessKeyID)
		}
		// `awsSecretAccessKey` optional argument
		if !querybuilder.IsZeroValue(opts[i].AwsSecretAccessKey) {
			q = q.Arg("awsSecretAccessKey", opts[i].AwsSecretAccessKey)
		}
		// `awsRegion` optional argument
		if !querybuilder.IsZeroValue(opts[i].AwsRegion) {
			q = q.Arg("awsRegion", opts[i].AwsRegion)
		}
		// `awsBucket` optional argument
		if !querybuilder.IsZeroValue(opts[i].AwsBucket) {
			q = q.Arg("awsBucket", opts[i].AwsBucket)
		}
		// `artefactsFqdn` optional argument
		if !querybuilder.IsZeroValue(opts[i].ArtefactsFqdn) {
			q = q.Arg("artefactsFqdn", opts[i].ArtefactsFqdn)
		}
		// `awsEndpointUrl` optional argument
		if !querybuilder.IsZeroValue(opts[i].AwsEndpointURL) {
			q = q.Arg("awsEndpointUrl", opts[i].AwsEndpointURL)
		}
		// `dryRun` optional argument
		if !querybuilder.IsZeroValue(opts[i].DryRun) {
			q = q.Arg("dryRun", opts[i].DryRun)
		}
	}
	q = q.Arg("tag", tag)
	q = q.Arg("commit", commit)
	q = q.Arg("githubOrgName", githubOrgName)

	return &Directory{
		query: q,
	}
}

// CliDevPublishMetadataOpts contains options for CliDev.PublishMetadata
type CliDevPublishMetadataOpts struct {
	AwsEndpointURL string
}

func (r *CliDev) PublishMetadata(ctx context.Context, awsAccessKeyId *Secret, awsSecretAccessKey *Secret, awsRegion string, awsBucket string, awsCloudfrontDistribution string, opts ...CliDevPublishMetadataOpts) error {
	assertNotNil("awsAccessKeyId", awsAccessKeyId)
	assertNotNil("awsSecretAccessKey", awsSecretAccessKey)
	if r.publishMetadata != nil {
		return nil
	}
	q := r.query.Select("publishMetadata")
	for i := len(opts) - 1; i >= 0; i-- {
		// `awsEndpointUrl` optional argument
		if !querybuilder.IsZeroValue(opts[i].AwsEndpointURL) {
			q = q.Arg("awsEndpointUrl", opts[i].AwsEndpointURL)
		}
	}
	q = q.Arg("awsAccessKeyId", awsAccessKeyId)
	q = q.Arg("awsSecretAccessKey", awsSecretAccessKey)
	q = q.Arg("awsRegion", awsRegion)
	q = q.Arg("awsBucket", awsBucket)
	q = q.Arg("awsCloudfrontDistribution", awsCloudfrontDistribution)

	return q.Execute(ctx)
}

// Verify that the CLI builds without actually publishing anything
func (r *CliDev) ReleaseDryRun(ctx context.Context) error {
	if r.releaseDryRun != nil {
		return nil
	}
	q := r.query.Select("releaseDryRun")

	return q.Execute(ctx)
}

func (r *CliDev) Tag(ctx context.Context) (string, error) {
	if r.tag != nil {
		return *r.tag, nil
	}
	q := r.query.Select("tag")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *CliDev) Version(ctx context.Context) (string, error) {
	if r.version != nil {
		return *r.version, nil
	}
	q := r.query.Select("version")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// CliDevOpts contains options for Query.CliDev
type CliDevOpts struct {
	RunnerHost string

	Source *Directory
	// Base image for go build environment
	Base *Container
	// Version of the Dagger CLI being built. Surfaced as CliDev.Version and
	// consumed by the publish flow (goreleaser ENGINE_VERSION, S3 paths,
	// semver release-gating). The built binary self-reports its own version
	// from the embedded internal/version/VERSION file regardless of what's
	// passed here, but this decides which engine the binary provisions by
	// default: a valid semver means a tag build (embedded VERSION already
	// matches, enforced by the publish workflow guard); anything else is a
	// commit build, whose default engine tag is pinned to the commit.
	Version string
	// Workspace whose git info stamps the CLI's VCS metadata and pins the
	// default engine tag on commit builds. Auto-injected when cli-dev is
	// called directly; a parent toolchain (e.g. engine-dev) instead resolves
	// it to the scalar vcsCommit/vcsDirty below and forwards those, so the
	// session-scoped Workspace never taints the cached build.
	Ws *Workspace
	// Resolved VCS commit to stamp, forwarded by a parent toolchain. Takes
	// precedence over ws.
	VcsCommit string
	// Resolved VCS dirty state to stamp, paired with vcsCommit.
	VcsDirty bool
}

func (r *Query) CliDev(opts ...CliDevOpts) *CliDev { // cli-dev (../../../../:0:0)
	q := r.query.Select("cliDev")
	for i := len(opts) - 1; i >= 0; i-- {
		// `runnerHost` optional argument
		if !querybuilder.IsZeroValue(opts[i].RunnerHost) {
			q = q.Arg("runnerHost", opts[i].RunnerHost)
		}
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `base` optional argument
		if !querybuilder.IsZeroValue(opts[i].Base) {
			q = q.Arg("base", opts[i].Base)
		}
		// `version` optional argument
		if !querybuilder.IsZeroValue(opts[i].Version) {
			q = q.Arg("version", opts[i].Version)
		}
		// `ws` optional argument
		if !querybuilder.IsZeroValue(opts[i].Ws) {
			q = q.Arg("ws", opts[i].Ws)
		}
		// `vcsCommit` optional argument
		if !querybuilder.IsZeroValue(opts[i].VcsCommit) {
			q = q.Arg("vcsCommit", opts[i].VcsCommit)
		}
		// `vcsDirty` optional argument
		if !querybuilder.IsZeroValue(opts[i].VcsDirty) {
			q = q.Arg("vcsDirty", opts[i].VcsDirty)
		}
	}

	return &CliDev{
		query: q,
	}
}
