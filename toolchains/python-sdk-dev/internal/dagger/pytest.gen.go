// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"dagger.io/dagger/querybuilder"
)

// The `PytestID` scalar type represents an identifier for an object of type Pytest.
type PytestID string // pytest (../../../../:0:0)

// Retrieve the binding value, as type Pytest
func (r *Binding) AsPytest() *Pytest { // pytest (../../../../:0:0)
	q := r.query.Select("asPytest")

	return &Pytest{
		query: q,
	}
}

// Create or update a binding of type Pytest in the environment
func (r *Env) WithPytestInput(name string, value *Pytest, description string) *Env { // pytest (../../../../:0:0)
	assertNotNil("value", value)
	q := r.query.Select("withPytestInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired Pytest output to be assigned in the environment
func (r *Env) WithPytestOutput(name string, description string) *Env { // pytest (../../../../:0:0)
	q := r.query.Select("withPytestOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

type Pytest struct { // pytest (../../../../:0:0)
	query *querybuilder.Selection

	id   *PytestID
	test *Void
}

func (r *Pytest) WithGraphQLQuery(q *querybuilder.Selection) *Pytest {
	return &Pytest{
		query: q,
	}
}

// Optional: A custom container with Python and uv installed.
// If not provided, defaults to Alpine Linux with uv.
func (r *Pytest) Container() *Container { // pytest (../../../../:0:0)
	q := r.query.Select("container")

	return &Container{
		query: q,
	}
}

// A unique identifier for this Pytest.
func (r *Pytest) ID(ctx context.Context) (PytestID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response PytestID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Pytest) XXX_GraphQLType() string {
	return "Pytest"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Pytest) XXX_GraphQLIDType() string {
	return "PytestID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Pytest) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Pytest) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Pytest) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = *dag.LoadPytestFromID(PytestID(id))
	return nil
}

// Optional: The source directory containing your Python project
func (r *Pytest) Source() *Directory { // pytest (../../../../:0:0)
	q := r.query.Select("source")

	return &Directory{
		query: q,
	}
}

// PytestTestOpts contains options for Pytest.Test
type PytestTestOpts struct {
	//
	// Additional arguments to pass to pytest (e.g., ["-x", "--tb=short"])
	//
	Args []string // pytest (../../../../:0:0)
	//
	// Python version to use (e.g., "3.12", "3.11", "3.10")
	//
	Version string // pytest (../../../../:0:0)
}

// Test a Python project with pytest and OpenTelemetry tracing.
//
// This function automatically injects pytest_otel for test tracing visibility
// in Dagger TUI and Dagger Cloud. No configuration required.
//
// The function will:
// - Use the provided container or default to Alpine Linux with uv
// - Install your project dependencies using uv sync
// - Inject pytest_otel for automatic test tracing
// - Run pytest with the specified Python version
func (r *Pytest) Test(ctx context.Context, opts ...PytestTestOpts) error { // pytest (../../../../:0:0)
	if r.test != nil {
		return nil
	}
	q := r.query.Select("test")
	for i := len(opts) - 1; i >= 0; i-- {
		// `args` optional argument
		if !querybuilder.IsZeroValue(opts[i].Args) {
			q = q.Arg("args", opts[i].Args)
		}
		// `version` optional argument
		if !querybuilder.IsZeroValue(opts[i].Version) {
			q = q.Arg("version", opts[i].Version)
		}
	}

	return q.Execute(ctx)
}

// Load a Pytest from its ID.
func (r *Query) LoadPytestFromID(id PytestID) *Pytest { // pytest (../../../../:0:0)
	q := r.query.Select("loadPytestFromID")
	q = q.Arg("id", id)

	return &Pytest{
		query: q,
	}
}

// PytestOpts contains options for Query.Pytest
type PytestOpts struct {
	//
	// Optional: The source directory containing your Python project
	//
	Source *Directory // pytest (../../../../:0:0)
	//
	// Optional: A custom container with Python and uv installed.
	// If not provided, defaults to Alpine Linux with uv.
	//
	Container *Container // pytest (../../../../:0:0)
}

func (r *Query) Pytest(opts ...PytestOpts) *Pytest { // pytest (../../../../:0:0)
	q := r.query.Select("pytest")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `container` optional argument
		if !querybuilder.IsZeroValue(opts[i].Container) {
			q = q.Arg("container", opts[i].Container)
		}
	}

	return &Pytest{
		query: q,
	}
}
